# Robogame — dev log

The session log has moved to [docs/changes/](docs/changes/).

- Start with [docs/changes/README.md](docs/changes/README.md) for the
  index (newest first).
- [docs/changes/architecture.md](docs/changes/architecture.md) is the
  current architecture snapshot.

Each prior session is its own file under `docs/changes/NN-slug.md`,
oldest first. Add a new file rather than appending to an existing one.
