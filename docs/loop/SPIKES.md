# SPIKES — one line per spike (D4 SPIKE tier). Append-only; never red-teamed; never a gate; never merged.

Line format: `N. [TAG] question — source — what was tried (branch/command, n) — answer ± D6 band — VERDICT — lesson` (≤ 900 characters; the cap is a ceiling, not a target).
Verdicts: KEEP (→ CHANGE-QUEUE with a draft acceptance criterion) / DROP / NOTE (a fact, not a candidate) / BLOCKED (names the missing tool, asset or capability) / PARK (needs a PLAY verdict or a DECIDE from Grey; reopens when it lands).
The cheapest kill is a pillar check, then a count, then a build. A spike branch is deleted or archived after its line is written; it never grows into the feature.

1. [PARK] Can the foreground move to the hive by hosting MCP for Unity's Python server there and pointing a desktop Editor at it (option C)? — LOOP-STATE BACKLOG 8; Grey 2026-09-16 "spike C later" — nothing tried (ASSUMPTIONS #3–#5 unverified) — no answer — PARK until Grey opts in; reopens without ceremony — lesson: none yet.
2. [OPEN] docs/subsystems/spherical-arenas.md describes planet arenas as unbuilt while PlanetArenaController / PlanetBody / PlanetGravityBody ship (F-012): rewrite in place, split, or move to historical? — doc-drift sweep 2026-09-16 — not yet tried; the cheapest step is a one-page current-state read of the three classes against the doc's sections — no answer — OPEN (next shift, rung 7) — lesson: none yet.
3. [KEEP] What is the run-to-run spread of `SurfaceNetsBenchmarkTests` Dim33 median on the test rig, and what gate follows from it (F-021)? — red team CHG-003 2026-09-17 (1.005 ms once during a full PlayMode run, pass on rerun) — tried: the fixture alone via `-testFilter`, 6 runs on the synced rig (main @ 1aaa7337), medians read from the batch logs (`.utmp/factory/sweeps/spike-l3-surfacenets-20260917.txt`; the log says dim=34, the test name says 33) — answer: isolated medians n=6 min=0.487 median=0.570 max=0.609 ms (mean 0.565); the 1.005 ms came from a run inside the full PlayMode suite, i.e. under load from the surrounding tests, so the gate's failure is a load artifact, not the remesh — KEEP → CHG-012: keep the 1 ms target as the number Grey set, but assert it against a band measured on this rig (target + the max-min spread of a 6-run isolated series, ≈ the value above), and log the median as a harness row so drift is visible; alternatively run the benchmark in its own PlayMode run — lesson: a perf assert inside a shared PlayMode run measures the neighbours too (D5: same rig, same load).
4. [PARK] Where does the music Grey hears during factory shifts come from, and does a batch Editor need a mute at all? — Grey's INBOX steer 2026-09-17T02:33Z — tried: `RigAudioMuteTests.BatchMode_HasEditorAudioMuted` on the rig BEFORE any fix: `EditorUtility.audioMasterMute` already reads true in batch (EditMode 534/534 with the test alone), FMOD's bus mirrors it at init (RuntimeManager.cs:532/1513) and MusicConductor mirrors it on its first Update (:257-260); a throwaway FMOD-output diagnostic did not compile (the test asmdef has no FMODUnity reference) — answer: the batch mute is already on; the audible source is unmeasured — PARK on PT-001 (Grey's ear names the process) — lesson: a rig cannot hear; when the acceptance is a sound, the test is a person.
5. [KEEP] What is the SurfaceNets Dim median UNDER FULL PLAYMODE LOAD, the condition in which it failed once (L3 measured it isolated)? — F-021, CHG-012 input — tried: 5 full PlayMode runs through run-tests.sh on main after CHG-010 (2026-09-17T16:2xZ; `.utmp/factory/sweeps/spike-l3b-surfacenets-load-20260917.txt`), medians read from the batch logs — answer: under load n=5 min=0.360 median=0.361 max=0.376 ms (mean 0.364), LOWER than isolated, gate verdicts 5 Passed; isolated (L3) 0.487–0.609 ms — KEEP → CHG-012: the gate must be judged in the condition it runs in; keep the 1 ms gate (2.6x margin over the worst measured median), add ~10 untimed warm-up iterations before timing, and print the median as a harness row so drift is a number, not a coin — lesson: load was NOT the cause (under full-suite load the median is lower than isolated); the one 1.005 ms failure followed a full recompile, so a cold Burst job is the likely cause; a benchmark that times from its first iteration measures the compiler once.
6. [NOTE] What leaks the 847 Persistent allocations the factory Editor reports at every domain reload (F-032)? — console sweep 2026-09-17 — tried: NativeLeakDetection.Mode = EnabledWithStackTrace via execute_code, then two domain reloads (a test-assembly recompile and a forced CleanBuildCache full recompile at 18:32Z); Editor.log carries exactly two "Leak Detected : Persistent allocates 847 individual allocations" lines, both 847, neither followed by a stack — answer: the same fixed set every reload, i.e. allocated once at Editor startup before the mode was set (a package or editor tool, not per-reload growth from Robogame code); a stack needs the leak-detection pref set BEFORE the Editor launches (Preferences > Jobs > Leak Detection Level, persisted in EditorPrefs) and a relaunch — NOTE (editor-only, constant, no growth; revisit if the count ever moves) — lesson: EnabledWithStackTrace only stacks allocations made after it is set; set it before launch, not mid-session.
7. [OPEN] Does the first post-recompile SurfaceNets window (F-038: 0.94 ms best, two windows over 1 ms, passing by best-of-three) reproduce when Library/ScriptAssemblies is deleted before a full PlayMode run, and is Burst compiling other assemblies' jobs the cause? — red team CHG-012, 2026-09-17 — not yet tried — no answer — OPEN (rung 9) — lesson: none yet.
8. [NOTE] What touches Physics during the batch Editor's -quit teardown after a player build (F-059)? — CHG-030's gate 2026-09-18 — tried (shift 9, 2026-09-18T22:30Z): read the log before the first error (no managed stack: the errors start right after the post-build ProjectSettings reimport, 8× "Failed to resolve instance id" then the PhysicsManager lines, then "[Physics::Module] Cleanup" and the Fatal Error); then a warm build with `EditorApplication.Exit(0)` called from the tool on success (.utmp/factory/spike-L8/build-exit0.sh, rig copy only) — answer: same crash, exit 21, 34 error lines, row Succeeded (282,090,814 B, 14.4 s): Exit(0) runs the same native teardown; no Robogame code is on the path — NOTE: an engine teardown-order fault in 6000.4.4f1 after BuildPlayer under -nographics; not tried: without -nographics (a window on Grey's screen) — lesson: judge a batch build by its report row; make build-player.sh exit 0 on a Succeeded row with this signature and say so.
