# ASSUMPTIONS — the register (D10)

Every load-bearing assumption about the engine, the platform, the rig, the player and the pipeline: unverified / verified / falsified. Unverified assumptions about BEHAVIOR block landing. Unverified assumptions about TASTE block nothing until the playtest; they are the playtest question. Reviewed at every landing and at the start of every shift.

| # | Assumption | Kind | Status | Evidence / plan to verify |
|---|---|---|---|---|
| 1 | PhysX diverges run-to-run for a fixed seed even on one machine, so a recorded-input replay cannot be an exact oracle | behavior | unverified | replay the same 60 s scripted input 5× on this rig; measure position divergence over time; decide the tolerance a replay test may use (BACKLOG 7) |
| 2 | Batch `-nographics` perf rows measure CPU, physics and GC only; render numbers need a graphics session | behavior | unverified | compare a harness row from `run-tests.sh` with one from the live Editor on the same scene |
| 3 | Two Unity Editors (Grey's and the factory's) fit in the desktop's RAM and both register on one MCP server with instance routing | behavior | unverified | open both, `set_active_instance`, `read_console` on each. Data point 2026-09-16: the factory's Editor alone reached 2.1 GB RSS while importing the clone |
| 4 | The Editor's MCP auto-start does not fight a remote server URL, and the plaintext opt-in works over Tailscale | behavior | unverified | the C spike (BACKLOG 8); PARKED |
| 5 | MCP for Unity's Python server at v9.7.3 can be hosted off the desktop with `--http-host` and the desktop Editor connects outbound | behavior | unverified | the C spike; the package's remote-server guide says so for the current version |
| 6 | `/loop` arms from the launcher's initial prompt argument | behavior | verified (Desktop route) | 2026-09-16: `/robogame-factory` armed the `loop` skill in a Claude Desktop session and the loop ran under it; the Windows-Terminal-tab route (`Start-Factory.ps1` without `-Desktop`) is still untested |
| 7 | Grey's playtest is the only feel instrument; no proxy substitutes | taste | verified by charter | END GOAL |
| 8 | The Desktop session's MCP client dials UnityMCP once at session start; a server that comes up later is unreachable until `reconnect_session_connector` runs at a turn end | behavior | verified | 2026-09-16: session created 23:34:33Z, Editor launched by the preflight at 23:34:43Z, UnityMCP `failed` for the whole session, 8080 not listening at 23:40Z; `reconnect_session_connector` REFUSES it ("UnityMCP is not a connector", 2026-09-17T01:48Z): a Desktop session that dialled before the Editor served 8080 has no bridge for its whole life; only sequencing fixes it (BACKLOG 12) |
| 9 | The plan's cap state is readable from inside a Desktop session (`get_usage`: per-window percent, extra-usage flag), so a cap trip is detectable by the loop and D-001's default can be enforced without a human at the terminal | behavior | verified | 2026-09-16 23:38Z: weekly all-models 100 %, weekly Fable 69 %, extra usage enabled; read by the loop and acted on (shift ended) |
| 10 | `Blueprint_DefaultBuggy` is a preset the scaffolder produces, so the suite may expect it on disk | behavior | falsified | 2026-09-16: GameplayScaffolder.cs:38-39 records that session 61 retired the Buggy preset, and the asset is not in the repo; `PresetBlueprintTests` still lists it and is Inconclusive on it every run (F-016, CHG-003) |
| 11 | The factory's GUI Editor can open the clone at all | behavior | falsified so far | three launches on 2026-09-16/17: one crash in InitializeAssetDatabaseV2 (corrupt ArtifactDB, since wiped), two hangs right after licensing with no UPM server, no window and a frozen log (before and after the wipe); the batch Editor opens the test-rig worktree fine. A modal dialog at startup is the leading guess; needs eyes on the desktop screen (NEEDS-GREY D-005) |
