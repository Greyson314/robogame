# ASSUMPTIONS — the register (D10)

Every load-bearing assumption about the engine, the platform, the rig, the player and the pipeline: unverified / verified / falsified. Unverified assumptions about BEHAVIOR block landing. Unverified assumptions about TASTE block nothing until the playtest; they are the playtest question. Reviewed at every landing and at the start of every shift.

| # | Assumption | Kind | Status | Evidence / plan to verify |
|---|---|---|---|---|
| 1 | PhysX diverges run-to-run for a fixed seed even on one machine, so a recorded-input replay cannot be an exact oracle | behavior | unverified | replay the same 60 s scripted input 5× on this rig; measure position divergence over time; decide the tolerance a replay test may use (BACKLOG 7) |
| 2 | Batch `-nographics` perf rows measure CPU, physics and GC only; render numbers need a graphics session | behavior | unverified | compare a harness row from `run-tests.sh` with one from the live Editor on the same scene |
| 3 | Two Unity Editors (Grey's and the factory's) fit in the desktop's RAM and both register on one MCP server with instance routing | behavior | unverified | open both, `set_active_instance`, `read_console` on each. Data point 2026-09-16: the factory's Editor alone reached 2.1 GB RSS while importing the clone |
| 4 | The Editor's MCP auto-start does not fight a remote server URL, and the plaintext opt-in works over Tailscale | behavior | unverified | the C spike (BACKLOG 8); PARKED |
| 5 | MCP for Unity's Python server at v9.7.3 can be hosted off the desktop with `--http-host` and the desktop Editor connects outbound | behavior | unverified | the C spike; the package's remote-server guide says so for the current version |
| 6 | `/loop` arms from the launcher's initial prompt argument | behavior | verified (Desktop route) | 2026-09-16: `/robogame-factory` armed the `loop` skill in a Claude Desktop session and the loop ran under it; the Windows-Terminal-tab route (`Start-Factory.ps1` without `-Desktop`) is still untested |
| 7 | Grey's playtest is the only feel instrument; no proxy substitutes | taste | verified by charter | END GOAL |
| 8 | (shift 8, 2026-09-18T04:47Z: the other half PROVEN: with CHG-020's server-first launcher (server already on 8080, Editor registered, preflight `bridge live` in 0.0 s) the FRESH Desktop session opened afterwards reported UnityMCP `connected` at its first and only dial, and the whole shift ran on the connector; BACKLOG 12 closed both ways.) (shift 7, 2026-09-17: SUPPORTED again — this session dialled hours before 8080 came up and stayed `failed` all shift while the bridge itself was live; `mcp_http.py` carried every bridge call of the shift, so the assumption now costs nothing) The Desktop session's MCP client dials UnityMCP once at session start; a server that comes up later is unreachable until `reconnect_session_connector` runs at a turn end. NARROWED 2026-09-17T18:05Z: this holds only for a session whose FIRST dial failed; a session that connected once recovers by itself when the server returns on 8080 (shift 5: the server died at 12:54Z, was restarted by hand at 18:04Z, and the next `read_console` from the same session succeeded) | behavior | verified (narrowed) | 2026-09-16: session created 23:34:33Z, Editor launched by the preflight at 23:34:43Z, UnityMCP `failed` for the whole session, 8080 not listening at 23:40Z; `reconnect_session_connector` REFUSES it ("UnityMCP is not a connector", 2026-09-17T01:48Z): a Desktop session that dialled before the Editor served 8080 has no bridge for its whole life; only sequencing fixes it (BACKLOG 12). Upstream cause found 2026-09-17: the package's `AutoStartOnLoad` pref was off, so no launched Editor served 8080 at all (LOOP-STATE § RIG); with it on, the sequencing worked on the first try in shift 5 (bridge up for the whole session) |
| 9 | The plan's cap state is readable from inside a Desktop session (`get_usage`: per-window percent, extra-usage flag), so a cap trip is detectable by the loop and D-001's default can be enforced without a human at the terminal | behavior | verified | 2026-09-16 23:38Z: weekly all-models 100 %, weekly Fable 69 %, extra usage enabled; read by the loop and acted on (shift ended) |
| 10 | `Blueprint_DefaultBuggy` is a preset the scaffolder produces, so the suite may expect it on disk | behavior | falsified | 2026-09-16: GameplayScaffolder.cs:38-39 records that session 61 retired the Buggy preset, and the asset is not in the repo; `PresetBlueprintTests` still lists it and is Inconclusive on it every run (F-016, CHG-003) |
| 11 | The factory's GUI Editor can open the clone at all | behavior | verified | 2026-09-17: the fourth launch (after the Library ArtifactDB/SourceAssetDB wipe) opened the clone and imported it (shift 4); the relaunch at 17:36Z opened it again and served MCP on 8080; shift 5 read its console and ran a menu item over the bridge (instance `robogame-factory@0de2e8ec2dc2f03e`). The two earlier hangs (2026-09-16/17, frozen log after licensing, no window) stay unexplained; if it hangs again, wipe the two Library databases and relaunch once before asking (D-005's rule) SHIFT 11 (2026-09-19): a cause that fits both early hangs was SEEN: after an unclean exit Unity opens on a modal "Recovering Scene Backups" (Yes/No) before any main window; pid 17068 sat on it for 15 min at 1 s CPU. Answered Yes by a window message, the Editor then loaded normally (F-066) |
| 12 | MCP for Unity's HTTP auto-start (`HttpAutoStartHandler`, a `delayCall`) does not fire while a freshly launched Editor sits unfocused; it fires once the window is brought to the foreground | behavior | supported (3 observations) | observed twice 2026-09-17 (two launches silent on 8080 for 8+ min in the background; 8080 up within a minute of foregrounding once); third observation shift 6: the launcher started the Editor at 21:47:18Z unfocused, 8080 was silent at 21:47:33Z and ~21:50Z with the Editor fully loaded (2.8 GB), the window was foregrounded at 21:51:04Z and 8080 listened at 21:51:19Z. Not yet isolated (a load that happens to finish at the nudge is the alternative); the launcher's 8080 wait (BACKLOG 12) should foreground the window once as a matter of course FOURTH observation, shift 11: a nudge on the SPLASH window (title 'Unity') did nothing in 600 s; one nudge on the MAIN window ('<project> - <scene> - … - Unity 6.x') attached in under 6 s. The launcher now nudges only the main window |
| 13 | The factory Editor's MCP client re-attaches by itself to a server that (re)appears on 8080 | behavior | falsified (narrowed) | holds ONLY while the Editor's bridge is still running (shift 5: the server died and came back, the client reconnected in a minute). When the package detects that the server IT launched (handshake-tracked) has died, it ends the session as orphaned and stops the bridge (Editor.log 2026-09-18: "Server no longer running; ending orphaned session" → "Bridge is not running"); a replacement server is then invisible until a domain reload re-runs the auto-start handler (which needs a refresh: this Editor has Auto Refresh off, `kAutoRefreshMode=0`) or someone presses Connect in the MCP window. Verify the swap path CHG-028b proposes at the next shift start before trusting it. 2026-09-18T22:21Z shift 9: the Editor had been closed between shifts; the launcher started a hand-started server, then a FRESH Editor (pid 22784), foregrounded it once, and `bridge live` printed after 147 s: the fresh-launch route works; Ctrl+R was not tried |
| 14 | The launcher's own answer to Unity's "Recovering Scene Backups" dialog (`GetDlgItem` id 1 + `WM_COMMAND`, Start-Factory.ps1 `Show-EditorOnce`) works from the launcher's process | behavior | unverified | shift 11 did the same by hand from another PowerShell process (button hwnd from UI Automation) and the dialog closed; the launcher's path first runs at the next unclean Editor exit and prints `answered Unity's 'Recovering Scene Backups' dialog` when it does |
| 15 | The D-007 login task (`RobogameFactory-McpServer`) serves 8080 after a real logon | behavior | unverified | installed 2026-09-19T20:13Z on Grey's click; `McpLoginTask.ps1 -Run` is a verified no-op while 8080 listens; the start path at a logon is unproven until Grey next logs in: at the next shift start read `Get-ScheduledTaskInfo RobogameFactory-McpServer` (LastTaskResult 0) and `.utmp/factory/mcp-server-login.err.log` |
