# LOOP-STATE (Robogame Factory) — read first, write last (every wake)

Directive version: v1.5 (I1 WIDENED; D8 S2 feature slot; D14 rung 1b). Last touched: 2026-09-19T20:40Z (end of shift 11, on the desktop).

## NEEDS GREY

The board is NEEDS-GREY.md (D12). This section only carries a pointer and the count: 0 APPROVE / 2 PLAY (PT-002 the bomb-bay door cue, PT-003 the kill-feed concoction name) / 0 BUY / 1 DECIDE (D-010: accept ADR-0005 as implemented?) / 39 FYI as of 2026-09-19T20:40Z (end of shift 11). Shift 11 worked off everything Grey answered on 2026-09-19T05:34Z: D-009 (207), CHG-004 (208), D-008 (210), D-007 (installed on his click at 20:13Z; the board carries the one removal command). PT-001 stays parked, D-006 stands down. D14's backpressure does not apply: PLAY 2 of 4.

## SHIFT (D13; replaces the kernel's LIVENESS block while the factory runs in shifts)

Lock: `.utmp/factory/shift.json` (written by Start-Factory.ps1: start time, mode, maxHours, model, effort; the end-of-shift routine copies it to `shift-last.json` with `ended` and deletes it).
Tick: `.utmp/factory/loop-tick.txt` (one line per real wake: UTC, what this wake did).
Session: a Claude Desktop session titled "Robogame factory" on the clone (launched via `/robogame-factory`, which runs `Start-Factory.ps1 -Desktop` then arms `/loop`), or the Windows Terminal tab the launcher opens (untested route, ASSUMPTIONS #6). Keep it open; detach nothing, close nothing.
Model policy (D11): foreground fable @ xhigh (Grey, 2026-09-16; confirmed by the session's own metadata on 2026-09-16); fieldhands per .claude/agents/ (sonnet, effort set per agent); red team opus @ high. Overage: D-001 ANSWERED 2026-09-17 ("it can" use usage credits): the wake routine still reads the plan's usage (`get_usage`) and records the windows in the tick and the ledger; no daily dollar ceiling was stated, so the D16d ceiling (4M tokens a shift) is the bound (LESSONS § METHOD 1).
Shared account: the Cosmonaut (bootleg_botlet, on the hive) runs on the same Max plan; every cap is shared. On 2026-09-16 the weekly all-models window was already at 100 % when shift 2 began.
Session-bound clocks: none. A nightly shift, when Grey wants one, is a Task Scheduler job that runs Start-Factory.ps1 (README).
Hot-file size budget (D13): ~200 KB; 2026-09-16 preflight measured 56 KB. Start-Factory.ps1 measures it at preflight.
Last commit known to this instance: shift 2 started at fd9be978 (main = origin/main); the shift's own commits follow it ("factory: shift 2 …").

## THE BUILD (D2 — main's current evidence; "unknown" is a legal, honest state until measured)

main @ end of shift 11 (2026-09-19T20:35Z): after CHG-037 (207), CHG-004 (208), CHG-039 (209), CHG-038 (210) = origin/main; `run-tests.sh --at HEAD All` on the MERGED main: **EditMode 584/584, PlayMode 166/167 (0 failed; the one documented skip)** (`.utmp/factory/suite-shift11-merged.log`); the factory Editor on merged main: 0 console errors; console sweep 2026-09-19 shift 11: 0 errors, 0 warnings on the load of all six shipped scenes, Arena play mode clean (`.utmp/factory/sweeps/console-2026-09-19-shift11.md`). Runtime code touched this shift: AudioCue + the AudioRouter.OneShotRequested seam + BombBayDoors.Drop (CHG-004), the SP kill-credit chain ProjectileSpec → DamageAttribution → MatchStatsTracker → ArenaController → KillFeedHud (CHG-039); each red-teamed PASS. Packages: com.unity.postprocessing removed, scripting defines now empty (CHG-038). Player build @ b9a210bb: Succeeded, 281,916,035 B, 49.3 s; headless first run 30 s: 0 errors, 0 exceptions, 0 warnings. NOT proven by any proxy: how the door cue sounds (the sound pack is absent from the clone: PT-002) and how the kill-feed suffix reads (PT-003). PREVIOUS: main @ end of shift 10 (2026-09-19T05:37Z): after CHG-006 (203), CHG-034 (204), CHG-036 (205), CHG-035 (206) = origin/main; `run-tests.sh --at HEAD All` on the MERGED main: **EditMode 575/575, PlayMode 165/166 (0 failed; the one documented skip)** (`.utmp/factory/suite-shift10-end.log`); the factory Editor recompiled merged main with 0 console errors; console sweep 2026-09-19: 0 errors, 0 warnings on the load of all six shipped scenes, no new findings (`.utmp/factory/sweeps/console-2026-09-19.md`). Runtime code touched this shift: AtomicFile + the three save sites (CHG-006), CommandLineMute + four mute gates (CHG-034), Tweakables.PersistenceSuspended (CHG-036), LabController → LabCanvasBuilder (CHG-035, zero behaviour change); each red-teamed PASS. Player build on main @ 85202018: Succeeded, 282,093,848 B, 43.0 s (warm Library after a rig reset). FIRST HEADLESS FIRST RUN (B3): 30 s, Bootstrap → MainMenu, 0 errors, 0 exceptions, muted by `-factory-mute` (`.utmp/factory/first-run/20260919T052700Z/Player.log`; F-065 the FMOD listener notice). The suite no longer writes Grey's real tweakables.json (F-063 → CHG-036; stamp unchanged across two full runs). Older evidence (shifts 8–9): docs/loop/shift-log-archive.md.
Suite history: shift 8 start (main @ e2d2cf6b): EditMode 543/543, PlayMode 152/153; per-branch gates this shift: 543/152 (026), 557/152 (025), 546/152 (027), 544/153 (024, +1 PlayMode); merged main above. 2026-09-17T22:23Z after CHG-019: **EditMode 542/542** (0 inconclusive; +5 ContinualTracesTests), **PlayMode 152/153** (1 skip: `MatchFlowTests.SpawnBot_ResultingGameObject_HasResolvedIInputSource`, documented, BACKLOG 2), **0 failures**. The `SurfaceNetsBenchmarkTests` coin (F-021) is fixed at its cold-cache cause by CHG-012 (docs/changes/181); the post-recompile residual is F-038 (SPIKES L7 owed). Wall time 82 s (red team CHG-015's measurement) against the 1m28s baseline with the package; EditMode alone 16–23 s. Results: `.claude/worktrees/test-rig/TestResults-{EditMode,PlayMode}.xml`.
Perf (docs/perf-captures/harness-log.txt): **SETTLED GARAGE BAND (shift 8, 2026-09-18T04:58–05:01Z, live factory Editor over the bridge, `perf_band.py --scene Garage`, 600 frames, two warm-ups discarded then five, Editor UNFOCUSED for all seven runs):** avg 2.09–2.60 ms (spread 0.51), median 1.97–2.43 ms (spread 0.46), p99 3.50–4.91 ms (spread 1.41), p99.9 4.41–7.28 ms (not a gate), GC 0 B/frame on all five; rows 2026-09-18 00:00:04–00:01:17 local; the August 2026 Garage row (avg 1.88 ms) was a different Editor state and is not compared. **SETTLED ARENA BAND (shift 7, 2026-09-17T17:48–17:50 local, live factory Editor over the bridge, `run_tests` PlayMode `PerfBaselineHarness.Arena_Idle_Baseline`, 600 frames, two warm-up runs discarded then five; BACKLOG 21):** avg 2.05–2.35 ms (spread 0.30), median 2.01–2.13 ms, p99 2.99–4.29 ms (spread 1.30), p99.9 3.6–8.9 ms (not a gate), GC 0 B/frame on all five; the Editor was unfocused at run 1 and focused by run 7 (the harness records no focus column yet; add one before the next band). Use THIS as the band; the shift-5 series below was a warming trend. Shift-5 series: Arena idle, live factory Editor, 2026-09-17T18:42–18:45Z, 5 runs back to back over the bridge (`run_tests` PlayMode `PerfBaselineHarness.Arena_Idle_Baseline`, 600 frames each): avg 3.94 / 3.44 / 3.20 / 2.73 / 2.66 ms, median 3.69 → 2.53 ms, p99 7.23 → 4.24 ms, p99.9 4.6–12.7 ms, GC 0 B/frame on all five. FIRST BAND, with its caveat (D6): the series trends down monotonically, i.e. the Editor was still warming across the runs, and the Editor's focus flipped to true at run 4; treat avg 2.7–3.9 ms (spread 1.3 ms) and p99 4.2–7.2 ms (spread 3.0 ms) as the band until a settled series (two discarded warm-up runs, then five) replaces it; the tail (p99.9) is not usable as a gate at this spread. The August 2026 rows (avg 5.13 ms, p99 6.12, p99.9 8.50) were a different Editor state and are not compared. Garage idle (2026-08-17) avg 1.88 ms, p99 2.56 ms; render probe 592 blocks chassis-in-view 2.19 ms. Budget: 16.6 ms target / 33 ms cliff (best-practices § 16). SurfaceNets mesher: `[SURFACENETS-BENCH]` rows since CHG-012 (batch rig: 0.36 ms warm, 0.54–0.66 ms cold cache, 0.94 ms first run after a recompile, F-038).
Soak: none exists. Last playtest: session 172's two user reports (grapple on trees; pogo tune), both closed in 172.
Console on scene load: **0 errors, 0 warnings on all six shipped scenes** (console sweep over the live bridge 2026-09-17, F-030; T2 done). One warning after a domain reload in the factory Editor: "Leak Detected: Persistent allocates 847 individual allocations" (F-032, spike armed). Invariants: INV-1/2/3/4/9 hold by code evidence, INV-5/6 spot-checked, INV-7/10 not evaluated (F-020).
Known weaknesses: the two player-facing landings of shift 11 are unheard and unseen by a person (PT-002, PT-003); F-070 (a player-typed concoction name reaches the kill-feed label: rich text and wrapping unchecked); F-012 (spherical-arenas.md describes a feature that shipped under other names, S1 doc; SPIKES L2 open); F-033 (a structure cube's plank grain follows its Up; Grey's art domain); F-038 (the post-recompile SurfaceNets window, passes by best-of-three; SPIKES L7 owed); F-057 (closed by CHG-028, docs/changes/200; the launcher's live server swap is unproven, ASSUMPTIONS 13); B3's remainder (garage → arena needs a scripted route in a headless run); F-065 (FMOD listener notice, SPIKES L9); F-064's residual (two rig scripts that both see a free rig in the same 15 s can still collide: a lock file would close it); the S1 queue holds no named extraction (F-049, F-052, F-053 landed: the next census names the next ones); the rest in FINDINGS.md. The visual audit has no rulebook until D-006 (art-direction.md is in style exploration mode).

## RIG (D7/D11 — where the truth test runs; one job at a time unless proven parallel-safe)

Machine: Grey's Windows desktop (Tailscale `desktop-p2msnrt`). Unity 6000.4.4f1 at `C:/Program Files/Unity/Hub/Editor/6000.4.4f1/Editor/Unity.exe` (hard-coded in run-tests.sh). Python 3.14.0 (`python` on PATH), git 2.54, `claude` at `~/.local/bin`.
Checkout: the factory's own clone, `C:\Users\Grey\Desktop\mutedtuple\robogame-factory`. Grey's checkout at `C:\Users\Grey\Desktop\mutedtuple\robogame` is never edited by the factory. Clone-local rig config: `.vscode/settings.json` is `skip-worktree` (LESSONS § METHOD 3).
Suite: `.claude/scripts/run-tests.sh [EditMode|PlayMode|All]` from the clone root (Git Bash); syncs the clone's working tree into `.claude/worktrees/test-rig` (exists, Library warm) and runs Unity batch `-nographics`. Measured 2026-09-16: All = 1m28s warm. Ran concurrently with the factory's Editor importing the clone, without incident (one data point).
Perf harness: PlayMode tests `[Category("Perf")]` in Assets/_Project/Tests/PlayMode/Perf/ (`PerfBaselineHarness.Arena_Idle_Baseline`, `Garage_Idle_Baseline`; `PerfRenderProbe.Arena_ChassisRenderCost_Attribution`); rows append to docs/perf-captures/harness-log.txt. In `-nographics` the render probe is meaningless; CPU/physics/GC rows stand. Graphics numbers need the live Editor.
MCP: `.mcp.json` → `http://127.0.0.1:8080/mcp`, served by whichever Editor auto-started MCP for Unity's server. The factory's Editor: `Unity.exe -projectPath <clone>` (Start-Factory.ps1 launches it; 2.1 GB RSS while importing). **The Desktop session dials the server once at session start** (ASSUMPTIONS #8): if the Editor was not serving 8080 by then, the bridge stays down for that session's whole life: `reconnect_session_connector` refuses project servers (2026-09-17) and `/mcp` is not available in a Desktop session. Sequencing is the only fix (BACKLOG 12). ROOT CAUSE FOUND 2026-09-17T17:05Z (Grey's question, outside a shift): MCP for Unity only serves 8080 when its EditorPrefs `MCPForUnity.AutoStartOnLoad` is true (HttpAutoStartHandler, default false) — the pref was never set, so NO Editor launched by the launcher ever served 8080 by itself; 8080 was only ever up when Grey pressed Start in the MCP window of his own Editor. Set to 1 in the registry (`HKCU\Software\Unity Technologies\Unity Editor 5.x`, key suffix `_h<djb2-xor hash>`; per-user, so Grey's Editor auto-connects too and the loop must keep using `set_active_instance`), the factory Editor was closed cleanly and relaunched: 8080 came up at 17:36Z (server pid 3728, Editor pid 25208) only after the Editor window was brought to the foreground once (two launches sat 8+ min in the background with the handler silent; delayCall appears not to tick for an unfocused fresh Editor — observed, not proven); the bridge was then proven end-to-end over HTTP (initialize → execute_code → mute=True on the clone's path) without a Desktop session. Habit that makes shifts smooth: leave the factory Editor open between shifts; the launcher only starts one when 8080 is silent. When Grey's Editor is also up, target this clone's instance with `set_active_instance`, never Grey's. PROVEN 2026-09-17 shift 5: the bridge was up from the first wake; instance id `robogame-factory@0de2e8ec2dc2f03e` (the hash is stable across server restarts); `read_console`, `execute_menu_item`, `execute_code`, the instances resource and two sweeps (console, visual) ran over it. **THE RIG KILLS THE BRIDGE (F-031, LESSONS § METHOD 9):** every `run-tests.sh` batch run ends with the package's quit cleanup terminating the factory Editor's MCP server (the "server I launched" handshake lives in per-user EditorPrefs), so 8080 goes silent after the first batch run of a shift. Until CHG-015 lands (the rig strips the package), restart the server by hand when `netstat -ano | findstr :8080` shows no listener: `uvx --offline --from mcpforunityserver==9.7.3 mcp-for-unity --transport http --http-url http://127.0.0.1:8080 --project-scoped-tools`, headless, output to `.utmp/factory/mcp-server-manual.log`; the Editor reconnects within a minute and THIS session's MCP client recovers by itself (ASSUMPTIONS #8 narrowed). A hand-started server carries no handshake, so a batch quit does not find it (to be confirmed by the next batch run). **SHIFT 8 (2026-09-18): the bridge died at ~05:25Z (F-057: one direct rig run without the strip killed the shift-6 handshake-tracked server) and STAYED DEAD for the shift: a hand-started replacement (pid 17948) listens on 8080 but the Editor ended its session as orphaned and never re-attached (LESSONS 15, ASSUMPTIONS #13); this Editor has Auto Refresh OFF (`kAutoRefreshMode=0`, per-user pref shared with Grey's Editor: it imports only on `refresh_unity` or Ctrl+R), and a fullscreen game on Grey's desktop held focus against four nudges. Next shift: the launcher finds 8080 served by the hand-started server and the Editor open; the Editor still needs ONE domain reload to attach (a `refresh_unity` will not work until it does): if `bridge live` does not print at preflight, the cheapest fix is Grey pressing Ctrl+R once in the factory Editor, or a fresh Editor launch by the launcher with the Editor closed first. CHG-028 makes the strip unavoidable and swaps in a hand-started server at every preflight.**
Blender: `.mcp.json`'s blender server; connected on 2026-09-16 (31 tools) but unused.
Discord: `.env` present since 2026-09-17 (D-003 "yes"; DISCORD_WEBHOOK_FACTORY verified by name and length, never by value) → `ping.py` posts for real.
Must NOT be open while a batch run executes: nothing (the test-rig worktree has its own Library); the test-rig worktree itself is never opened in an Editor.
Editor faults on this clone: shift 1's Editor crashed in InitializeAssetDatabaseV2 with 'Assertion failed: mv_size == sizeof(T)' and 'dataValue.mv_size >= sizeof(ArtifactMetaInfo)' (Editor-prev.log 2026-09-16T20:48Z; crash report %LOCALAPPDATA%/Temp/Unity/Editor/Crashes/Crash_2026-09-16_204843616), i.e. a corrupt Library/ArtifactDB or SourceAssetDB; shift 2's Editor hung after 'Input System module state changed: Initialized' with no window for 2 h and was killed 2026-09-17T01:53Z. Fix applied 2026-09-17: Library/ArtifactDB, Library/SourceAssetDB and Library/Artifacts deleted (derived caches; a full reimport runs on the next launch, so the bridge comes up late in shift 3). The launch after the wipe (2026-09-17T01:47Z, pid 29144) hung the same way: log frozen after licensing, no UPM server started (upm.log shows only the batch run's), no window, killed at 01:53Z. So the Library was not the hang's cause. Leading guess: a modal dialog at startup that a GUI launch shows and a batch launch does not. NEEDS-GREY D-005 asks Grey to look at the screen once; until then every shift is batch-only.

SHIFT 11 (2026-09-19): the launcher's fresh Editor sat 15 min on Unity's modal "Recovering Scene Backups" (it appears after an unclean exit, before any main window: it fits the D-005 hangs) and the one focus nudge was spent on the splash. Both fixed in Start-Factory.ps1 (F-066; ASSUMPTIONS 11, 12, 14): the launcher answers Yes (backups copied to the gitignored Assets/_Recovery/) and nudges only the main window. D-007 is INSTALLED (20:13Z, Grey's click): scheduled task `RobogameFactory-McpServer` runs `.claude/scripts/factory/McpLoginTask.ps1 -Run` at logon and hand-starts the server unless 8080 listens (ASSUMPTIONS 15: the logon path is unproven until his next login). After a manifest change do NOT `refresh_unity` with `compile: request` (LESSONS 20). This session's connector was `failed` all shift (the server started after the dial); everything ran over `mcp_http.py`.

## FACTORY FLOOR (D16e — v1, published 2026-09-16; iterate)

WAKE ROUTINE (every wake, in order, cheap): (1) `git fetch` + INBOX log between HEAD and origin; (2) plan usage (`get_usage`): record the windows in the tick; usage credits are allowed (D-001, 2026-09-17) and the D16d per-shift ceiling binds; (3) bridge check (`session_connectors_status`; reconnect if `failed` and 8080 listens); (4) the suite in the background if the tree changed since the last green; (5) stamp the tick.
STANDING SWEEPS (D4; `sweeper` on sonnet/medium, one sweep per call, ≤ 5 sweeps a shift per LESSONS § METHOD 4): console + suite-and-perf every shift (console needs the bridge); doc-drift, best-practices, invariants weekly (run 2026-09-16; next due 2026-09-23); visual on every visible landing and weekly (bridge); provenance on every import and monthly (run 2026-09-16); readiness weekly; /ideate through design-pilot when APPROVE has < 2 open and the queue's feature lane is empty.
FIELDHANDS: per D11 tiers. A landing costs qa-verifier + perf-checker (when hot) + red-team; budget the D16d ceiling as ≤ 5 sweeps + ≤ 3 gates a shift, or fewer sweeps when the queue is deep.
SHIFT SHAPE: wake routine → rung 0 (suite) → ladder (D14) with the queue built through the landing chain (`land.py gate` / `land.py land`) → END-OF-SHIFT ROUTINE: LOOP-STATE (§ SHIFT LOG bullet ≤ 600 chars, § NEXT ITEM), FINDINGS/SPIKES/CHANGE-QUEUE/LAUNCH-READINESS/ASSUMPTIONS/LESSONS current, NEEDS-GREY counts, the report through `ping.py` (dry until D-003), the tick, the lock → `shift-last.json` + delete, commit "factory: shift N end …", push, a one-line push notification, the loop stopped.
STANDING PRIORITIES (CHARTER D8 SUGGESTIONS S1, Grey 2026-09-17): every shift's mix carries at least one simplification, deletion, de-duplication or modularization change (AUTO when the suite proves zero behaviour change) or a health/perf item, not only findings-driven fixes; the best-practices sweep runs with the spaghetti lenses (duplication, god classes, cross-module reach-through, dead fields such as F-037) and proposes the deletion or split as the action; the suite-and-perf sweep judges health. Candidates on file: F-037 (three dead tuning fields), F-027/CHG-009 (46 obsolete-API sites), the idea of a code-size census (lines per assembly, largest classes, duplicate blocks) as the first sweep of the next shift.
FEATURE SLOT (CHARTER D8 S2 + D14 rung 1b, Grey 2026-09-19): every shift's mix carries ONE feature-lane item: a spec, a build or an /ideate pass. Pre-approved, no second check-off, one PLAY question each: Rider Effects and Concoction Identity (idea-backlog § Approved; the Lab they were queued behind shipped in session 114). Route: planner → spec in CHANGE-QUEUE → tests first → build → gate with a red team → land with a PLAY entry (cap 4). INV-8 gap polish on a shipped mechanic is AUTO now (I1 WIDENED i): land, FYI, revert on Grey's word.
MOLT: when the hot set passes 200 KB or the context shows rot (D11), write state and restart the session.

## HEALTH (D16c; shift 11, 2026-09-19)

Shift 11 (17:49Z–20:40Z, of which 18:06Z–20:13Z idle behind a question card: LESSONS 19): landed 4 (CHG-037, 004, 039, 038 = docs/changes/207–210) + 5 factory tooling commits (McpLoginTask.ps1 + the launcher's dialog answer and main-window nudge; inbox_mark.py, 6 tests first; land.py PLAY count, test first; specs; findings); findings surfaced 5 (F-066–F-070), acted on 5 (F-008, F-029, F-060, F-066, F-067), specced 5 (CHG-037, 038, 039, 040; CHG-004 amended); bugs closed with tests 1 (land.py counted the PLAY format template as an open question); readiness items moved 1 (C2 unknown→open with two cue guards) + new points on B2 and B3; board entries answered by Grey 1 (the D-007 install click); FIRST PLAYER-FACING FEATURE landed by the factory (CHG-039; the feature slot's first use). Delegation mix: 2 planners sonnet (55k, 83k), 2 builders general-purpose sonnet (244k, 180k), 3 red teams sonnet/medium (36k, 51k, 66k), 1 sweeper sonnet/medium 33k ≈ 748k fieldhand tokens, 8 sessions of the D16d 8 (the ceiling met: the shift ended on it); D-009, D-008, the tooling and all state built in the foreground; foreground fable/xhigh context 38 % of 1M at the end-of-shift write; harness stubs 0. Pipeline: two builders and a planner at once from the first wake, then three red teams and a player build queued on the rig with no collision. Changes landed/week with evidence: 29 (13 on 09-17, 8 on 09-18, 8 on 09-19).

Shift 10 (04:54Z–05:40Z): landed 4 (CHG-006, 034, 036, 035 = docs/changes/203–206) + 3 factory tooling commits (first-run.sh; run-tests.sh rig wait F-064; shift log archive); findings surfaced 3 (F-063, F-064, F-065), acted on 6 (F-053, F-062, F-063, F-064 landed or fixed; F-060(a) found already done; F-048 re-read: 592 of 592), specced 4 (CHG-034, 006, 035, 036); bugs closed with tests 2 (F-063 via CHG-036; T4's atomic writes via CHG-006); readiness items moved 3 (T4 unknown→open with its first half done; B3 first capture; B2 third build point); board entries answered by Grey 1 (the four levers → CHARTER v1.5); delegation mix: 1 sweeper sonnet/medium 45k, 3 builders general-purpose sonnet (147k, 228k, 383k), 4 red teams sonnet/medium (47k, 84k, 56k, 83k) ≈ 1.07M fieldhand tokens, 8 sessions of the D16d 8 (the ceiling met: the shift ended on it); CHG-036 built in the foreground; foreground fable/xhigh context 34 % of 1M at the end-of-shift write; harness stubs 0. Pipeline: three builders at once from the first wake, gates and a player build sharing the rig; one rig collision (F-064, fixed; LESSONS 16). The connector was CONNECTED from the first dial (MCP tools all shift); the bridge survived every batch run. Changes landed/week with evidence: 25 (13 on 09-17, 8 on 09-18, 4 on 09-19).

Shift 9 (22:21Z–23:10Z): landed 4 (CHG-031, 028, 032, 033 = docs/changes/199–202) + 3 factory tooling commits (build-player.sh row verdict; land.py F-061 with a regression test; index title F-056); findings surfaced 3 (F-060, F-061, F-062), acted on 7 (F-049, F-052, F-055 DROP, F-056, F-057, F-059→L8 NOTE, F-061), specced 3 (CHG-031, 032, 033); bugs closed with tests 1 (F-061: land.py dropped a branch's own edits to the shared files; CHG-025's 19 index rows restored); readiness items moved 0 (B3 checked: blocked on F-062); board entries answered by Grey 0; delegation mix: 1 sweeper sonnet/medium 63k, 3 builders general-purpose sonnet (145k incl. one resume, 177k, 155k), 2 red teams sonnet/medium (58k, 47k) ≈ 645k fieldhand tokens, 6 sessions of the D16d 8; foreground fable/xhigh context 25 % of 1M at the end-of-shift write; harness stubs 0. Pipeline: up to two lanes at once (a red team and a builder sharing the rig through run-tests.sh's own wait); the bridge ran over mcp_http.py all shift (connector failed at the dial, launcher bridge live) and survived every batch run. Changes landed/week with evidence: 21 (13 on 09-17, 8 on 09-18).

Shift 8: landed 6 (CHG-026, 025, 027, 024, 029, 030 = docs/changes/193–198); findings surfaced 11 (F-049–F-059), acted on 12 (F-029→D-008, F-035, F-039, F-044, F-047, F-048 NOTE, F-050, F-051, F-054, F-057→CHG-028 spec, F-058→CHG-030 landed, F-059→SPIKES L8), specced 7 (CHG-024, 025, 026, 027, 028, 029, 030) + CHG-004 (ASK→APPROVE); bugs closed with tests 1 (F-058, the player build, fixed with the build as its test); readiness items moved 4 (T1→done, B1 unknown→done, B2 unknown→open with first numbers, B3 unknown→open with a plan); board entries answered by Grey 0; delegation mix: 1 sweeper sonnet/medium 139k, 6 builders general-purpose sonnet (295k, 346k, 346k, 359k handed off under the 400k ceiling, 251k, 182k), 2 red teams sonnet/medium (75k, 108k) ≈ 2.10M fieldhand tokens; foreground fable/xhigh context 51 % of 1M at the end-of-shift write; harness stubs 0; fieldhand sessions 8 of the D16d 8 (the ceiling met exactly: the console sweep was dropped, BRIDGE_DOWN). Pipeline: four lanes at once for most of the shift (one in the tree with the Editor, two by plumbing, one foreground), gates serialized on the rig by the -runTests predicate; the bridge died at 05:25Z (F-057) and stayed dead: one builder handoff, live dumps replaced by tests, the console sweep skipped, four focus nudges on Grey's screen (LESSONS 15). Changes landed/week with evidence: 17 (13 on 09-17, 4 on 09-18). Shift 7: landed 6 (CHG-021, 016, 009, 023, 022, 018 = docs/changes/187–192); findings surfaced 3 (F-046, F-047, F-048), acted on 6 (F-024, F-027, F-034, F-037, F-040, F-046), specced 3 (CHG-021, 022, 023); bugs closed with tests 1 (the render-probe flake, CHG-023); delegation mix: 1 sweeper sonnet/medium 105k, 3 builders general-purpose sonnet (157k, 130k, 243k), 3 red teams sonnet/medium (30k, 50k, 46k) ≈ 761k; foreground fable/xhigh context 47 % of 1M; harness stubs 0; the bridge over HTTP all shift (connector dead, launcher bridge live). Main green at the end: EditMode 543/543, PlayMode 152/153 (one timing flake seen once and fixed).
Changes landed/week with evidence: 13 (CHG-003, 002, 005, 001, 008, 010, 015, 012, 014, 013, 019, 017, 020; all 2026-09-17). Board entries answered/week: 2 (D-005 and CHG-013's APPROVE, Grey, 2026-09-17; D-001..D-004 the day before). Bugs closed with tests: 4 (F-016 via CHG-003; F-031 via CHG-015; F-021's cold-cache cause via CHG-012; F-041 via CHG-019, test first). Readiness items moved: 9 (unchanged this shift). Shift 6: findings surfaced 2 (F-044, F-045), acted on 3 (F-036, F-041, F-042), specced 2 (CHG-019, CHG-020); delegation mix: 2 builders (general-purpose sonnet, 212k + 213k), 0 sweeps, 0 red teams (D16b: nothing needed one), plus the peer runner session's CHG-020 build on its own ledger; foreground fable/xhigh context 66 % of 1M at the end-of-shift write; harness stubs 0. Grey's process changes this day: I1 dev-facing content AUTO and ties to landing, D16b red team narrowed to sonnet/medium for runtime code only, D8 S1 simplification/deletion priority, D12 Discord as a what-changed list. Shift 5: findings surfaced 13 (F-030–F-042), acted on 4 (F-023, F-030, F-031, F-021), specced 6 (CHG-012, 014, 015, 017, 018, 019). Pipeline occupancy: up to three lanes at once (a builder on the rig, a plumbing-built branch, a sweep in the Editor); gates serialized on the rig, named. Delegation mix shift 5: 2 × sweeper sonnet/medium (39k + 63k), 1 × builder (general-purpose sonnet, 158k), 4 red-team passes opus/high (CHG-013 102k, CHG-015 93k, CHG-012 89k, CHG-014 81k) ≈ 625k fieldhand tokens; foreground fable/xhigh context 51 % of 1M at the end-of-shift write. Harness stubs: 0. Earlier shifts' numbers follow.
Main: green (0 failures). Suite: 683 cases, 1m28s warm. Perf vs budget: last rows 2026-08-17, Arena idle avg 5.13 ms of 16.6 ms; band unmeasured. Board: 4 DECIDE open, oldest D-001 (2026-09-16). Findings surfaced 20 / acted on 1 (F-017, the command fix) / specced 8 (into CHG-001..003). Pipeline occupancy: 1 lane at a time (the test rig syncs the single checkout, so gates serialize; named per D16). Shift 4 tokens: red team ×2 (84k + CHG-010's) + the fable foreground (context ~70 % at shift end); usage card delta about $29 (6386.83 → 6416.28) (shared account, upper bound). Delegation mix: 4 × sweeper sonnet/medium (~349k tokens measured: 71k + 74k + 100k + 104k); foreground fable/xhigh (context 164k at 23:38Z). Harness stubs: 0 seen. Tokens shift 2: ~349k fieldhand + the foreground (usage credits). Shift 3: red team ×4 passes (64k + 65k+84k + 62k + 86k+round 2) ≈ 360k opus + the fable foreground (context ~55 % of 1M at shift end); the usage card's extra-usage figure moved about $58 (6252.60 → 6310.75) during the shift (shared with the Cosmonaut: an upper bound).

## PROCESS WEIGHT (Grey, 2026-09-17 — read before proposing any new check)

The loop's validation culture came from a trading system where one wrong bar cost real money.
This is a casual solo game: a wrong landing costs a revert and a replay. Grey called the
overhead too much, so CHARTER I1 and D16b were amended in the same session: dev-facing content
(presets, fixtures, sample scenes, generated snapshots) is AUTO even when visible; the red team
runs only for shipped runtime code, the NOD list, or an invariant, at sonnet/medium unless an
invariant is touched. A tie between AUTO and ASK goes to LANDING. New process is a cost like any
other and needs a reason bigger than "it would be more rigorous".

## LANES IN FLIGHT (D4 CHANGE tier — each change's stage: SPEC / APPROVE-WAIT / TESTS / BUILD / GATE / LANDED; the named dependency if serialized)

- Landed: shift 3 CHG-003 (174), CHG-002 (175), CHG-005 (176), CHG-001 (177); shift 4 CHG-008 (178), CHG-010 (179); shift 5 CHG-015 (180), CHG-012 (181), CHG-014 (182); Grey's runner session CHG-013 (183); shift 6 CHG-019 (184), CHG-017 (185), CHG-020 (186, built by the peer session); shift 7 CHG-021 (187), CHG-016 (188), CHG-009 (189), CHG-023 (190), CHG-022 (191), CHG-018 (192). CHG-011 — PARK (PT-001). SHIFT 8 (2026-09-18): LANDED CHG-026 (193), CHG-025 (194), CHG-027 (195), CHG-024 (196), CHG-029 (197: the player-build instrument; its first build failed, F-058), CHG-030 (198: the one-line fix; the Windows player builds, B1 done). SHIFT 9 (2026-09-18): LANDED CHG-031 (199), CHG-028 (200), CHG-032 (201), CHG-033 (202). SHIFT 10 (2026-09-19): LANDED CHG-006 (203), CHG-034 (204), CHG-036 (205), CHG-035 (206). SHIFT 11 (2026-09-19): LANDED CHG-037 (207), CHG-004 (208), CHG-039 (209), CHG-038 (210). FEATURE LANE (D8 S2): Concoction Identity SHIPPED (CHG-039: only the kill-feed half was missing, ADR-0005 had shipped the rest); Rider Effects: CHG-040 SPEC (the additive Rider field, no behaviour, no UI), then CHG-041 (the Burn rider + Lab picker + ONE PLAY question) to spec through the planner; Smoke and EMP stall after it, one at a time. CHG-028 LANDED; its old line: SPEC (run-tests.sh --at + the launcher swaps in a hand-started server; F-057). Queued: CHG-006, CHG-007; F-049/F-052/F-053 extractions (S1); a PlayMode Collider assertion for CHG-027's MakeHighlightShell (owed, one test); SPIKES L2 (F-012), L7 (F-038). Serialization: the rig runs one batch at a time and run-tests.sh syncs the clone's checkout, so gates serialize; branches are built with plumbing (LESSONS 10) so building never waits on the checkout.

## BACKLOG (with priors; triage on merit; AUTO items → CHANGE-QUEUE, ASK items → NEEDS-GREY, unknowns → SPIKES)

1. ~~`FindObjectOfType` straggler in DevHud.cs~~ — gone from the code; the stale doc note is F-015 → CHG-002.
2. `MatchFlowTests.SpawnBot` is `[Ignore]`d, blocked on `Tests/Scenes/MinimalArena.unity` (the skip message names the trimmed GameStateController + one-block library it needs) — AUTO (coverage), M → CHG-006 to spec. Prior: high.
3. Tweakables defaults vs persisted JSON: a bumped default does not reach a user with a saved value (architecture.md gotcha) — readiness risk; SPIKE first (count the affected keys), then ASK if behavior changes. Prior: high.
4. Atomic writes (`.tmp` + `File.Replace`, best-practices § 11.3) for UserBlueprintLibrary.cs:147, ConcoctionLibrary.cs:122, Tweakables.cs:512 — readiness T4, AUTO once specced with a test → CHG-005. Prior: high.
5. Editor test asmdef `Robogame.Block.Tests` (best-practices § 14.3) — AUTO (coverage/tooling), M. Prior: medium.
6. A headless soak (arena, bots on `IInputSource`, N minutes, exceptions + GC/frame + p99) — instrument, rung 5; AUTO. Prior: high; the gate is weaker without it.
7. Recorded-input replay through `IInputSource` — instrument; blocked on ASSUMPTIONS #1. Prior: medium.
8. The C spike — SPIKES L1, PARK until Grey opts in.
9. idea-backlog § Approved: ~~Concoction Identity~~ SHIPPED shift 11 (CHG-039, docs/changes/209; PT-003); Rider Effects: pre-approved (I1 WIDENED ii), CHG-040 SPEC then CHG-041. Prior: Grey approved them 2026-06-04.
10. Launch-readiness sweep — partly done 2026-09-16 (T1, L1, L2, D1, D2); the rest of the `unknown` rows need the bridge (T2, C1, C2) or a build (B1–B5).
11. HANDOFF remainder: the perf harness Arena idle baseline 5× for the run-to-run band (§ RIG); prove `read_console` + `run_tests` against this clone's Editor instance once the bridge is up; record the instance id.
12. ~~Instrument: the launcher waits for 8080 before printing the prompt~~ — CHG-020 LANDED (186) and CLOSED 2026-09-17 shift 7: the launcher's first real run reported `bridge live: robogame-factory@0de2e8ec2dc2f03e` in 0.0 s (server and Editor were already up from shift 6); this session's own connector had dialled hours earlier and stayed `failed`, and the whole shift ran over `mcp_http.py` (mute, three scene loads, play, read_console, a forced recompile, seven harness runs) with no loss. The `UnityMCP: connected` half of the proof needs a session opened AFTER the launcher; the next fresh session records it. Still owed from the old item: the launcher muting the factory Editor (PT-001's factory side). S.
13. ~~F-008 bomb-bay door cue~~ — DONE shift 11 (CHG-004, docs/changes/208; PT-002).
14. F-012 spherical-arenas.md → SPIKES L2 (rung 7). L.
15. Console sweep + visual sweep — never run (bridge). First thing after the bridge is proven.
16. Rig: the startup hang has a seen cause since shift 11 (the "Recovering Scene Backups" modal, F-066) and the launcher answers it. Still owed: the launcher tails Editor.log for 'Crash!!!' or a stall and says so in the preflight.
18. CHG-008 (F-022): Grappler + HoverTank preset coverage, one list — AUTO, S. Then SPIKES L3 (F-021) before T1 can close.
19. docs/TRACES.md is stale in 17 sites (F-023): Robogame → Traces → Rebuild Index once the Editor opens (D-005); add TRACES.md to the doc-drift sweep. AUTO, S.
17. Instrument: `/inbox` commits to main with plumbing (`git commit-tree` + `update-ref refs/heads/main`, then push) instead of committing on the checked-out branch (LESSONS § METHOD 5). AUTO (tooling), S.
20. ~~CHG-015's true-positive run~~ — DONE 2026-09-17T22:0x–22:23Z: the shift-6 server on 8080 (pid 18704, auto-started with the handshake) survived every batch quit of the CHG-019 builder's runs and the shift's own suite runs (docs/changes/184 § Verification).
21. ~~BACKLOG 11 remainder: the Arena idle baseline 5× in the LIVE Editor~~ — DONE; the SETTLED Arena series landed 2026-09-17 shift 7 (§ THE BUILD; rows 0eae8a95; script: the runner session's perf_band.py, 2 warm-ups + 5 over `run_tests`, ~90 s). OWED: the Garage baseline the same way, and a focus column in the harness row. `run_tests` over the bridge, ~20 s a run; never while a batch run is on the rig. S.
22. F-032: read the leak's stack after the next domain reload in the factory Editor (`read_console` with stack traces, filter "Leak"); the allocations that leaked before the mode was set carry no stack. Then SPIKES L6. S.
23. ~~CHG-009 needs the full warning texts~~ — DONE 2026-09-17T18:33Z over the bridge: `execute_code` → `CompilationPipeline.RequestScriptCompilation(CleanBuildCache)` in the factory Editor, then `grep 'warning CS'` of Editor.log → `.utmp/factory/sweeps/warnings-census-editor-2026-09-17.txt` (127 sites, 46 first-party in 19 files; summary in the CHG-009 pending line). Rerunnable in one call; the rig route is not needed.
24. Visual sweep tooling (from the 2026-09-17 sweep): `manage_camera view_target` did not move the scene-view camera (two identical shots); Arena shots need an explicit `view_position` above the terrain; the HUD needs play mode, which the sweeper lacks. Fold into the sweep brief once D-006 gives it a rulebook. S.
26. ~~Console sweep DONE shift 9 (clean); the visual sweep stands down per D-006's default~~ Console sweep + visual sweep this shift: SKIPPED (BRIDGE_DOWN from 05:25Z, F-057); first thing next shift once `bridge live` prints. S.
27. ~~DONE shift 9 (CHG-031, docs/changes/199)~~ CHG-027 follow-up: one PlayMode test asserting MakeHighlightShell destroys the primitive's Collider in play mode (the EditMode test cannot; the red team's reading did). S.
28. ~~DONE shift 9: the Editor had been closed; the launcher's fresh launch attached in 147 s (ASSUMPTIONS 13)~~ Next shift start: the factory Editor (pid 11096) has a STOPPED MCP bridge and Auto Refresh OFF; the hand-started server (pid 17948) listens on 8080. The launcher reports `bridge live` only after the Editor attaches, which needs one domain reload: if the preflight warns, Grey presses Ctrl+R in the factory Editor once, or closes it and lets the launcher start a fresh one. Record which worked in ASSUMPTIONS #13. S.
25. Upstream: file the EditorPrefs-handshake defect with CoplayDev (mcp-for-unity v9.7.3: McpEditorShutdownCleanup + IPidFileManager), citing docs/changes/180; a one-paragraph issue. S; Grey may prefer to file it himself (his account).

## INSTRUMENTS (built, reusable)

- `.claude/scripts/run-tests.sh` — batch suite on the test-rig worktree (1m28s warm, 2026-09-16).
- Perf harness (Assets/_Project/Tests/PlayMode/Perf/) → docs/perf-captures/harness-log.txt.
- Robogame → Traces → Validate / Rebuild Index (Assets/_Project/Scripts/Tools/Editor/ContinualTraces.cs); file-level fallback: grep `TRACE\[` and resolve ids by hand (the doc-drift sweep does this when the bridge is down).
- `/ideate` (.claude/commands/ideate.md) with design-pilot; docs/research/idea-backlog.md as dedupe memory.
- `.claude/scripts/factory/`: Start-Factory.ps1, Stop-Factory.ps1, ping.py, land.py, discord_intake.py (+ tests: `python -m unittest discover -s .claude/scripts/factory/tests -t .claude/scripts/factory/tests` → OK on 2026-09-16).
- `/robogame-factory` (.claude/commands/robogame-factory.md): preflight → plan-cap check → bridge reconnect → arm /loop.
- `.claude/scripts/factory/perf_band.py --scene Arena|Garage [--warmups 2 --runs 5]` (CHG-026): the settled band over the bridge; waits for a free rig (`-runTests`), records focus, prints one BAND line to `.utmp/factory/perf-band-<scene>-<date>.txt`. Rig runs at a sha (LESSONS 10): `.claude/scripts/run-tests.sh --at <sha> [--label <name>] EditMode|PlayMode|All` (CHG-028): the ONLY road onto the rig; it strips the MCP package and waits for a free rig (exit 3 after RIG_WAIT_TIMEOUT_SECS). Builder and red-team briefs name it and nothing else.
- `.claude/scripts/factory/inbox_mark.py --match <text> --note <what the loop did> [--push]`: the ONLY way to mark an INBOX line handled; edits from `git show HEAD:`, never the working copy; refuses anything but a one-line suffix (LESSONS 18). `McpLoginTask.ps1 -Status|-Run|-Install|-Uninstall`: the D-007 login task.
- Session introspection from a Desktop session: `get_usage` (plan windows, context size), `get_session` (model, effort), `session_connectors_status` / `reconnect_session_connector` (the bridge).
- Agents: planner, design-pilot, test-drafter, qa-verifier, perf-checker, sweeper, red-team.

## BUDGET LEDGER (I2; tokens per shift under the D16d ceiling; purchases by proposal)

| Date | Item | Tokens / cost | Note |
|---|---|---|---|
| 2026-09-16 | charter + seed (on the hive) | ~0 to the factory | — |
| 2026-09-16 | shift 1 (fable/xhigh) | foreground only, state read; ended on STOP | — |
| 2026-09-16 | shift 2 (fable/xhigh + 4 sweeps) | fieldhands 349k measured; foreground context 164k at 23:38Z, output unmeasured | all usage credits: the weekly all-models window was at 100 % at shift start (D-001) |
| 2026-09-17 | shift 3 (fable/xhigh + 4 red-team gates) | fieldhands ≈ 360k measured (opus); foreground context ≈ 550k at shift end | usage credits (D-001 answered: allowed); usage card delta about $58 (6252.60 → 6310.75) (upper bound, shared account) |
| 2026-09-17 | shift 4 (fable/xhigh + 2 red-team gates, same session) | fieldhands ≈ 170k (opus); foreground context ≈ 700k at shift end | usage credits (D-001); usage card delta about $29 (6386.83 → 6416.28) (upper bound, shared account) |
| 2026-09-17 | shift 5 (fable/xhigh; 2 sweeps + 1 builder on sonnet, 4 red-team passes on opus) | fieldhands ≈ 625k measured; foreground context 511k (51 % of 1M) at the end-of-shift write | usage credits (D-001); plan windows at start and end: 5-hour 0 %, weekly all-models 100 %, weekly Fable 69 %; usage card delta about $52 (6454.04 → 6506.29) (upper bound, shared account) |
| 2026-09-17 | shift 6 (fable/xhigh, same session as shift 5 resumed; 2 builders on sonnet, 0 sweeps, 0 red teams) | fieldhands ≈ 425k measured; foreground context 664k (66 % of 1M) at the end-of-shift write; the peer session's CHG-020 build is on its own ledger | usage credits (D-001); windows: 5-hour 0 %, weekly all-models 100 %, weekly Fable 69 %; usage card delta about $75 (6529.58 → 6604.32) INCLUDING the peer session and Grey's CHG-013 landing session on the same account (upper bound) |
| 2026-09-17 | shift 7 (fable/xhigh, the game: Robogame factory session; 1 sweep + 3 builders on sonnet ≈ 635k, 3 red teams sonnet/medium ≈ 126k) | fieldhands ≈ 761k measured; foreground context 475k (47 % of 1M) at the end-of-shift write | usage credits (D-001); windows: 5-hour 0 %, weekly all-models 100 %, weekly Fable 69 %; usage card delta about $56 (6606.57 → 6662.98) (upper bound, shared account) |
| 2026-09-18 | shift 8 (fable/xhigh, Desktop, 04:47–07:10Z; 1 sweep + 6 builders on sonnet ≈ 1.92M, 2 red teams sonnet/medium ≈ 183k) | fieldhands ≈ 2.10M measured (8 sessions, the D16d ceiling); foreground context 51 % of 1M at the end-of-shift write | plan windows at start: 5-hour 0 %, weekly all-models 100 %, Fable 69 % (the week reset at 05:00Z); at end: 5-hour 27 %, weekly all-models 7 %, weekly Fable 10 %; usage card 6705.18 → 6732.49 (upper bound, shared account) |
| 2026-09-18 | shift 9 (fable/xhigh, Desktop, 22:21–23:10Z; 1 sweep + 3 builders on sonnet ≈ 540k, 2 red teams sonnet/medium ≈ 105k) | fieldhands ≈ 645k measured (6 sessions); foreground context 25 % of 1M at the end-of-shift write | plan windows at start: 5-hour 6 %, weekly all-models 14 %, weekly Fable 24 %; at end: 5-hour 10 %, all-models 15 %, Fable 25 %; usage card 6732.49 → 6732.49 (no extra usage drawn) |
| 2026-09-19 | shift 10 (fable/xhigh, Desktop, 04:54–05:40Z; 1 sweep + 3 builders on sonnet ≈ 803k, 4 red teams sonnet/medium ≈ 270k) | fieldhands ≈ 1.07M measured (8 sessions, the D16d ceiling); foreground context 34 % of 1M at the end-of-shift write | plan windows at start: 5-hour 0 %, weekly all-models 16 %, weekly Fable 27 %; at end: 5-hour 8 %, all-models 18 %, Fable 30 %; usage card 6732.49 → 6732.49 (no extra usage drawn) |
| 2026-09-19 | shift 11 (fable/xhigh, Desktop, 17:49–20:40Z with 18:06–20:13Z idle behind a card; 2 planners + 2 builders + 1 sweep on sonnet ≈ 595k, 3 red teams sonnet/medium ≈ 153k) | fieldhands ≈ 748k measured (8 sessions, the D16d ceiling); foreground context 38 % of 1M at the end-of-shift write | plan windows at start: 5-hour 0 %, weekly all-models 18 %, weekly Fable 30 %; at end: 5-hour 8 %, all-models 20 %, Fable 33 %; usage card 6732.49 → 6732.49 (no extra usage drawn) |

Purchases: none. Grey's measured usage goes beside every estimate here.

## OPEN JOBS (background — never foreground waits; long jobs list the successor queued behind them)

(none running at shift end. Left in place on purpose: the factory Editor pid 17068 (bridge live, instance robogame-factory@0de2e8ec2dc2f03e, Garage.unity loaded, merged main compiled with 0 errors, Auto Refresh off) and the hand-started MCP server on 8080 (pid 21596; no pidfile, no batch quit can kill it). Landed branches chg/004, chg/006, chg/009–039 can be archived under archive/. Unlanded: chg/011 (PARK). Untracked and gitignored on disk: the recoveries under Assets/_Recovery/ (left alone).)

## SHIFT LOG (this shift's ≤ 600-char bullets; `land.py` appends here; archive to docs/loop/shift-log-archive.md when past ~20 KB)

Shifts 1–9 (2026-09-16 to 2026-09-18): docs/loop/shift-log-archive.md (cold).

- 2026-09-19T05:14:37Z — CHG-006 Atomic save writes: blueprints, concoctions and tweakables go through .tmp + File.Replace — docs/changes/203-atomic-writes.md — CHG-006 landed (AUTO, readiness T4's atomic-writes half; red team sonnet/medium PASS 47k): AtomicFile.WriteAllText (.tmp, then File.Replace with a .bak, else File.Move; cleans the .tmp and rethrows on failure) replaces the bare File.WriteAllText in UserBlueprintLibrary, ConcoctionLibrary and Tweakables; no format change (Tweakables stays BOM-less); 5 temp-dir EditMode tests first (compile-red), incl. a locked-destination case on Unity Mono; suite at bf76f322 567/567 + 160/161. No test writes real saves (F-063).
- 2026-09-19T05:19:02Z — CHG-034 -factory-mute: a command-line flag silences every first-party audio path, so a headless player run is quiet — docs/changes/204-factory-mute.md — CHG-034 landed (AUTO, instrument flag; red team sonnet/medium PASS 84k): Robogame.Core.CommandLineMute (lazy cached whole-token parse of -factory-mute, one log line when seen) ORed into the four mute gates: MusicConductor, GarageMusic, AudioRouter, MusicMidi (the 4th found by the census); not a Tweakable, nothing changes without the flag; tests first (6 EditMode + 4 PlayMode, compile-red); suite at be4c488e 568/568 + 164/165. Unblocks B3 (F-062). Its test's Audio.Mute write is F-063's path: CHG-036 next.
- 2026-09-19T05:26:42Z — CHG-036 No test run writes the real tweakables.json: Tweakables.PersistenceSuspended + a SetUpFixture per test assembly — docs/changes/205-tweakables-persistence-seam.md — CHG-036 landed (AUTO, F-063 found and fixed this shift; built in the foreground; red team sonnet/medium PASS 56k): Tweakables.PersistenceSuspended gates Save(); a namespace-less SetUpFixture in each test assembly holds it for the run, so the suite no longer rewrites Grey's real tweakables.json (it did on every PlayMode run). Tests first (compile-red); suite at c869aa4e 575/575 + 164/165; the real file's stamp and size unchanged across a full run.
- 2026-09-19T05:35:50Z — CHG-035 LabCanvasBuilder: the Lab's one-shot UGUI scene construction leaves LabController — docs/changes/206-lab-canvas-builder.md — CHG-035 landed (AUTO, D8 S1 modularization; red team sonnet/medium PASS 83k): the Lab's one-shot canvas construction (ground, screws, header, journal, switchboard, vial) moved to a static LabCanvasBuilder; LabController 1,209 → 726 lines, Update untouched (INV-6); pinning PlayMode test green before and after with a byte-identical 134-line UGUI dump; before/after screenshots over the bridge match; suite at 5c19b216 562/562 + 161/162. F-053 closed.

- 2026-09-19T04:54Z–05:40Z — shift 10 (fable/xhigh, Desktop; connector connected, MCP tools all shift). Landed 4: CHG-006 (203, atomic save writes, T4 half), CHG-034 (204, -factory-mute), CHG-036 (205, the suite no longer writes the real tweakables.json: F-063 found, built and landed in-shift), CHG-035 (206, LabCanvasBuilder, 1,209 → 726). B3 first headless run clean to MainMenu. F-063–F-065; F-064 fixed; LESSONS 16–17. Grey's four levers → CHARTER v1.5. Ended on the D16d session ceiling (8 of 8).
- 2026-09-19T20:18:36Z — CHG-037 The five tracked crash-recovery scenes under Assets/_Recovery are deleted (D-009) — docs/changes/207-recovery-scenes-deleted.md — CHG-037 landed (NOD given: D-009 "delete"; red team sonnet/medium PASS 36k): the five tracked Assets/_Recovery crash-recovery scenes, their metas and the folder meta are gone (11 paths); .gitignore also ignores the folder's meta. 0 of 6 GUIDs referenced, none in build settings; suite at 732c3baf 575/575 + 165/166. F-060 closed.
- 2026-09-19T20:20:35Z — CHG-004 Bomb-bay doors: an audible open, a silent slam (INV-8 gap) — docs/changes/208-bomb-bay-door-cue.md — CHG-004 landed (Grey: "yes open, minimal or nothing for slam"; red team sonnet/medium PASS 51k): AudioCue.BombBayDoorOpen appended as the enum's last member (74), requested once per BombBayDoors.Drop(); wizard row + library entry by text, clip reused from UiToggleOn (USFX is absent from the clone: BuildAll never runs here); test seam AudioRouter.OneShotRequested; tests first; suite at 60c74ee7 577/577 + 166/167. Nobody has heard it yet: PT-002. F-008 closed.
- 2026-09-19T20:21:30Z — CHG-039 Concoction Identity, the missing half: the kill-feed names the concoction that made the kill — docs/changes/209-killfeed-concoction.md — CHG-039 landed (FEATURE SLOT, pre-approved Concoction Identity; red team sonnet/medium PASS 66k): name + tint had shipped under ADR-0005; this is the kill-feed half. An optional concoction name rides the SP kill-credit chain (ProjectileSpec.WeaponName → DamageAttribution → MatchStatsTracker → KillFeedHud), rendered as a "(name)" suffix, today's line when absent. No serialized field, no save change. Tests first (compile-red, 7 cases); suite at eb9a8f3a 582/582 + 165/166. PT-003.
- 2026-09-19T20:24:13Z — CHG-038 The unused Post Processing Stack v2 package and its scripting define are removed (D-008) — docs/changes/210-remove-ppv2.md — CHG-038 landed (ASK answered: D-008 "remove"; red team N/A): com.unity.postprocessing 3.5.4 out of the manifest and the lock; UNITY_POST_PROCESSING_STACK_V2 stripped from all 16 platforms (scriptingDefineSymbols: {}). Tests first by the existing guard: RED at commit 1 (574/575), green at b9a210bb 575/575 + 165/166; player build Succeeded 281,916,035 B (−178 KB), headless first run 0 errors. URP's own post stack untouched. F-029 closed.

- 2026-09-19T17:49Z–20:40Z — shift 11 (fable/xhigh, Desktop; connector failed, all over mcp_http.py). Landed 4: CHG-037 (207, D-009: recovery scenes deleted), CHG-004 (208, door open cue; PT-002), CHG-039 (209, the FIRST feature: the kill-feed names the concoction; PT-003), CHG-038 (210, D-008: PPv2 removed). D-007 installed on Grey's click. Launcher answers the recovery dialog, nudges the main window (F-066). inbox_mark.py; land.py PLAY count. Console sweep clean. F-066–F-070; D-010; LESSONS 19–20; CHG-040 SPEC. Two hours idle behind a card. Ended on the D16d ceiling (8 of 8).

## NEXT ITEM

Wake routine (expect `bridge live`; if the Editor opens on the recovery dialog the launcher answers it: record ASSUMPTIONS 14; read ASSUMPTIONS 15: `Get-ScheduledTaskInfo RobogameFactory-McpServer`). INBOX: `inbox_mark.py` is the only way to mark a line (LESSONS 18). Cards: only right after Grey typed, or as a wake's last act (LESSONS 19). After a manifest landing: LESSONS 20. Rung 0 (suite: expect 584/584 + 166/167). Board: PT-002 (door cue, unheard), PT-003 (kill-feed name, unseen), D-010 (ADR-0005's status) await Grey; FYI at 39; `decide D-010: accept` → flip the ADR's status line. Rung 1: F-070 (read KillFeedHud's entry style: rich text and wrapping for a player-typed name; one line + one test; runtime → red team). (1b) THE FEATURE SLOT: CHG-040 (SPEC ready: the additive Rider field, tests first, red team, FYI), then the planner on CHG-041 (the Burn rider: on-hit DoT where damage is computed, a lazily added tick component, a flat CPU addend, Lab picker None/Burn, a Burn VFX + cue; the loop picks the numbers and names them in the ONE PLAY question; PLAY would be 3 of 4). S1 slot: the next census (BlockEditor 1,124, SettingsHud 1,098, ArenaController ~1,199) names one extraction. Then: B3's remainder (a scripted headless route garage → arena: a `-factory-goto <scene>` flag or an IInputSource script; `-timestamps` in first-run.sh for B2's load time); C2's remainder (VFX coverage per block; the missing-cue logger over a full play session); SPIKES L9 (F-065), L2 (F-012), L7 (F-038); a rig lock file (F-064 residual; shift 11 queued four lanes on the rig with no collision: one data point); CHG-007 (MinimalArena); T4's remainder (format versioning; BACKLOG 3). Briefs: `.utmp/factory/builder-brief-shift11.md` is current (rules 9–11: no test writes persistentDataPath; never run AudioCueWizard.BuildAll in the clone, USFX is absent; a refused report.md comes back as the final message). `run-tests.sh --at <sha>` is the only road onto the rig; plumbing-built branches; every step of a multi-line call carries `|| exit 1`.

PREVIOUS (shift 10's, kept for the record): Wake routine (expect `bridge live` and, in a session opened after the launcher, `UnityMCP: connected`). INBOX discipline first (LESSONS 18: fetch and restore INBOX.md before touching it). GREY ANSWERED THE WHOLE BOARD (NEEDS-GREY § ANSWERED, 2026-09-19T05:34Z), so rung 1 is full: (i) D-009: `git rm` the five tracked `Assets/_Recovery/` scenes + metas (the nod is on file; suite as the gate); (ii) D-008: remove `com.unity.postprocessing` from the manifest + the `UNITY_POST_PROCESSING_STACK_V2` define line (gate: suite + `build-player.sh`); (iii) CHG-004: build it ("yes open, minimal or nothing for slam"): the open cue from a clip ALREADY in the clone, the slam minimal or silent, I6 row for anything new; INV-8 polish is AUTO now, FYI after landing; (iv) D-007 (yes; the decision is settled, the scribe attests Grey ticked it on a Desktop card, INBOX 2026-09-19T05:43Z): the loop installs the login task ITSELF (never hands Grey a command: zero terminal steps). At install time the runner puts up ONE AskUserQuestion card ("install the login task now?") and installs on the click: the runner's own rules do not take a peer session's attestation as its user's go-ahead for a persistent change to the PC; this is one click, not a second decision. Then the board note in plain language: what was installed and the ONE removal command (`Unregister-ScheduledTask`); (v) build `inbox_mark.py` (LESSONS 18) before the next INBOX edit. D-006: the visual sweep stands down. PT-001: CHG-011 stays PARK. Rung 0 (suite: expect 575/575 + 165/166). Then, in order: (1b) THE FEATURE SLOT, first time: Concoction Identity (idea-backlog § Approved: name + a WorldPalette tint on trail/explosion + kill-feed authorship; cosmetic, no gameplay stat; a palette index on Concoction is an ADDITIVE serialized field: prove an old concoction file still loads, the save format is on the NOD list): planner → spec → tests first → build → red team → land with ONE PLAY question; Rider Effects after it (gameplay: CPU-costed, server-computed, a serializer bump: spec the versioning with T4's remainder). Then: B3's remainder: a scripted headless route garage → arena (a `-factory-goto <scene>` flag or an IInputSource script) and `-timestamps` in first-run.sh for B2's load time; SPIKES L9 (F-065), L2 (F-012), L7 (F-038); a rig lock file (F-064 residual); the next S1 census (BlockEditor 1,124, SettingsHud 1,098, ArenaController 1,191; LabController is 726 now); CHG-007 (MinimalArena test scene for the one skipped test); T4's remainder (format versioning; BACKLOG 3 tweakables defaults). Board: CHG-004, PT-001, D-006–D-009 are with the scribe; FYI at 36. Briefs: `run-tests.sh --at <sha>` is the only road onto the rig; plumbing-built branches; no test writes persistentDataPath (LESSONS 17); never edit a rig script while a lane runs it (LESSONS 16); a subagent's harness may refuse to write report.md: take the report from its final message. After a scribe `/inbox`, local main moves under the checkout: `git restore --staged --worktree docs/loop/INBOX.md` when only INBOX shows as modified.
