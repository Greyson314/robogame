# FINDINGS — one line per finding (D4 SWEEP). Append-only; the loop's working memory of what is wrong or wanting on main.

Line format (pipes; one line; ≤ 600 characters):
`F-NNN | date | class | severity | where | what | evidence | proposed action | cost | disposition`
- class: bug / polish / visual / perf / inconsistency / best-practice / feature / purchase / readiness
- severity: S0 red main or crash · S1 player-visible defect · S2 quality or drift · S3 nit
- where: a path and line, a scene and object, a doc section
- evidence: a pointer, never a claim — path:line, a console line, a screenshot under .utmp/factory/sweeps/, a harness row
- cost: S / M / L
- disposition: AUTO→CHG-NNN · ASK→NEEDS-GREY · SPIKE→SPIKES L<n> · NOTE · DROP (reason)

A sweep that re-reports a finding already here is a defect in the sweep: grep before appending.

F-001 | 2026-09-16 | readiness | S2 | Assets/Stylized Nature Pack/ | third-party pack wired into the shipping arena scaffolder (fog, leaves, dust, stone props) with no license on disk and no row in art-direction.md § Imported Assets (I6) | Assets/_Project/Scripts/Tools/Editor/ArenaProps.cs:57 | record license + origin in art-direction.md § Imported Assets | S | ASK→D-004→CHG-001 LANDED 2026-09-17 (docs/changes/177-provenance-records.md)
F-002 | 2026-09-16 | readiness | S2 | Assets/Polytope Studio/ | lowpoly tree prefab wired into the arena prop scatter, no license on disk, no doc row (I6) | Assets/_Project/Scripts/Tools/Editor/ArenaProps.cs:276 | record license + origin in art-direction.md § Imported Assets | S | ASK→D-004→CHG-001 LANDED 2026-09-17 (docs/changes/177-provenance-records.md)
F-003 | 2026-09-16 | readiness | S2 | Assets/Handpainted_Grass_and_Ground_Textures/ | ground albedo wired into the Fluff arena grass material, no license on disk, no doc row (only the Fluff package is documented) | Assets/_Project/Scripts/Tools/Editor/FluffGround.cs:275 | record license + origin in art-direction.md § Imported Assets | S | ASK→D-004→CHG-001 LANDED 2026-09-17 (docs/changes/177-provenance-records.md)
F-004 | 2026-09-16 | readiness | S3 | Assets/FattyPolyTurretFree/, Assets/FattyPolyTurretPart2Free/ | two Asset Store turret demo packs, unreferenced by any script under Assets/_Project; FattyPolyTurretFree/Readme.txt is the Part2 pack's readme; no explicit license | Assets/FattyPolyTurretFree/Readme.txt | record license + origin, or delete (deleting assets needs Grey's nod, I1) | S | ASK→nod D-004→CHG-005 LANDED 2026-09-17 (docs/changes/176-delete-unused-packs.md)
F-005 | 2026-09-16 | readiness | S3 | Assets/Le Tai's Asset/TrueShadow/ | paid Asset Store UI package, unreferenced under Assets/_Project, no root license file (only bundled demo-content sub-licenses) | Assets/Le Tai's Asset/TrueShadow/ (no License.txt at the package root) | record license + origin, or delete (nod) | S | ASK→nod D-004→CHG-005 LANDED 2026-09-17 (docs/changes/176-delete-unused-packs.md)
F-006 | 2026-09-16 | readiness | S3 | artgen/ → Assets/_Project/Art/Models/**/*.fbx | 33 generated FBX outputs correlate to artgen/*.py only by filename convention; no manifest records generator script + Blender version (I6: generated assets record their generator) | Assets/_Project/Art/Models/Blocks/Inv/CapyCube_Inv.fbx; no artgen/README.md exists | add artgen/README.md mapping each FBX to its script | S | AUTO→CHG-001 LANDED 2026-09-17 (docs/changes/177-provenance-records.md)
F-007 | 2026-09-16 | readiness | S3 | Packages/manifest.json:3 | `com.coplaydev.unity-mcp` is a GitHub-pinned editor package with no origin or license row in docs/PACKAGE_MODIFICATIONS.md | Packages/manifest.json:3 | add the row (editor-only, non-shipping) | S | AUTO→CHG-001 LANDED 2026-09-17 (docs/changes/177-provenance-records.md; MIT, verified at the repo's LICENSE)
F-008 | 2026-09-16 | bug | S1 | Assets/_Project/Scripts/Combat/BombBayDoors.cs:1-48 | bomb-bay door swing (snap open, slam shut) has no audio cue and no VFX cue anywhere on the drop path; only the resulting explosion has AudioCue.BombExplosion (INV-8) | BombBayDoors.cs:8-9 ("Visual only" comment); BombBayBlock.cs:192 (the only AudioRouter/AudioCue reference in the bay/door pair) | declare a door open/close cue + a hinge VfxKind at Drop() (INV-8 allows declaring the cue before the clip exists) | S | ASK→spec CHG-004 next shift (audible + visible)
F-009 | 2026-09-16 | inconsistency | S3 | Assets/_Project/Scripts/Core/HudPointerGuard.cs:32 | dangling trace `DOC:best-practices§statics`: no such heading (2.4 Singletons and 2.5 Lifecycle exist) | HudPointerGuard.cs:32 | retarget to `DOC:CLAUDE.md§Known failure modes` | S | AUTO→CHG-002 LANDED 2026-09-17 (docs/changes/175-doc-drift-sweep.md). NOTE: the validator resolves the file only, never the heading, so this was misleading prose, not a dangling link (red team) | S
F-010 | 2026-09-16 | inconsistency | S2 | docs/subsystems/tip-blocks.md:16,31 | the leash diagram and table say damper = 250; the code zeroes it deliberately | Assets/_Project/Scripts/Movement/RopeBlock.cs:597-624 (`damper = 0f` at :624) | fix the doc: damper 0, deliberate, cite the line | S | AUTO→CHG-002 LANDED 2026-09-17 (three sites, not the two the locator named; red team caught :140)
F-011 | 2026-09-16 | inconsistency | S2 | docs/subsystems/scalable-parts.md § Status | header says "Plan. No code in this branch yet"; Phase 0/1/1.5 shipped | Assets/_Project/Scripts/Block/BlockOccupancy.cs:51; docs/changes/38-*.md, 39-*.md | rewrite the Status line to the shipped phases | S | AUTO→CHG-002 LANDED 2026-09-17
F-012 | 2026-09-16 | inconsistency | S1 | docs/subsystems/spherical-arenas.md (whole doc) | frames planet arenas as unbuilt; PlanetArenaController / PlanetBody / PlanetGravityBody ship and are never named | docs/changes/architecture.md:53 vs the doc's framing | rewrite as a current-state doc, or move to historical + a new subsystem doc | L | SPIKE→SPIKES L2
F-013 | 2026-09-16 | inconsistency | S3 | docs/changes/architecture.md:53 | lists `PlanetGravity` as an existing class; no such file, the gravity math is `GravityField` | GravityField.cs vs PlanetBody.cs:28-31 (comment-only mention) | replace with `GravityField` | S | AUTO→CHG-002 LANDED 2026-09-17
F-014 | 2026-09-16 | readiness | S2 | README.md:254-262,276 | "Last updated: April 29, 2026"; the Changelog still says "Initial project setup" (LAUNCH-READINESS D1) | README.md:276 | update the date; point the Changelog at docs/changes/README.md | S | AUTO→CHG-002 LANDED 2026-09-17
F-015 | 2026-09-16 | inconsistency | S3 | docs/best-practices.md:573-574,778-780 | § 12.5 and the open-items bullet cite a `FindObjectOfType` straggler at DevHud.cs#L125 that no longer exists (DevHud.cs has zero FindObject* calls today) | Assets/_Project/Scripts/UI/DevHud.cs (no match) | strike the stale note; closes BACKLOG 1 | S | AUTO→CHG-002 LANDED 2026-09-17
F-016 | 2026-09-16 | bug | S2 | Assets/_Project/Tests/EditMode/Blueprints/PresetBlueprintTests.cs:37 | `PresetPaths` lists Blueprint_DefaultBuggy.asset; the asset is not in the repo (GameplayScaffolder.cs:38-39: session 61 retired the Buggy preset), so the case is Inconclusive on every run (readiness T1); ScriptedChassisBuilderTests.cs:177 names the same path | .claude/worktrees/test-rig/TestResults-EditMode.xml 2026-09-16: "Preset asset not found at Assets/_Project/ScriptableObjects/Blueprints/Blueprint_DefaultBuggy.asset" | drop the stale path from the test list | S | AUTO→CHG-003 LANDED 2026-09-17 (docs/changes/174)
F-017 | 2026-09-16 | readiness | S2 | .claude/commands/robogame-factory.md; Start-Factory.ps1 -Desktop | the preflight starts the Editor after the Desktop session has already dialled UnityMCP once, so the bridge is down for the whole shift (no console or visual sweep, no MCP proof) | session_connectors_status 2026-09-16 23:38Z: UnityMCP `failed`; 8080 not listening at 23:40Z | reconnect + cap-check steps in the command (landed this shift, FYI-2); a launcher-side wait for 8080 (BACKLOG 12) | S | AUTO→landed (command edit, shift 2)
F-018 | 2026-09-16 | readiness | S2 | <clone>/.env | absent, so DISCORD_WEBHOOK_FACTORY is unset and every ping dry-runs (README § Desktop setup 3) | ping.py 2026-09-16: "DRY level=info parts=1" | Grey creates the webhook + .env, or answers "no Discord" | S | ASK→D-003 answered yes 2026-09-17; .env present (name + length verified); pings live
F-019 | 2026-09-16 | readiness | S3 | suite on main @ fd9be978 | 2026-09-16 test-rig run: EditMode 529/530 (1 inconclusive = F-016), PlayMode 152/153 (1 skip: MatchFlowTests.SpawnBot, documented, BACKLOG 2), 0 failed, 1m28s warm | .claude/worktrees/test-rig/TestResults-{EditMode,PlayMode}.xml | none; T1 evidence | — | NOTE
F-020 | 2026-09-16 | best-practice | S3 | docs/invariants.md vs Assets/_Project/Scripts | invariants sweep: INV-1 holds (DevTuningOverride.cs:40-106 gated + compile-stripped), INV-2 (GarageController.cs:194 is the sole BuildModeController.Enter call), INV-3 (ModuleSystem.cs:140 IsServer gate), INV-4 (five AddComponent<Rigidbody> sites, all documented free bodies or the ADR-0002 mirror), INV-9 (no additive terrain); INV-5/6 spot checks hold, not exhaustive; INV-7/10 not evaluated (harness / bridge) | the grep list is in the sweep file (.utmp/factory/sweeps/invariants-2026-09-16.md, gitignored; the pointers above are the durable part) | none | — | NOTE
F-021 | 2026-09-17 | bug | S2 | Assets/_Project/Tests/PlayMode/Voxel/SurfaceNetsBenchmarkTests.cs:58 | `Mesh_Dim33Sphere_BurstMedianUnderOneMillisecond` asserts a 50-iteration median < 1.0 ms as a hard machine gate; on the test rig it measured 1.005 ms once and passed on rerun (0.5 % over): a flaky test is a bug (D2) and a proxy without its band (D6) | red team CHG-003, 2026-09-17: PlayMode run 1 failed on it, run 2 passed (.utmp/factory/gate/CHG-003-redteam.md) | measure the median's run-to-run spread on the rig (10×), then set the gate = target + band, or make it relative to a stored baseline row | S | SPIKE→SPIKES L3 KEEP (isolated n=6 min=0.487 median=0.570 max=0.609 ms (mean 0.565); the failure was under full-suite load) → CHG-012
F-022 | 2026-09-17 | best-practice | S2 | Assets/_Project/Tests/EditMode/Blueprints/PresetBlueprintTests.cs:33-47 vs GameplayScaffolder.cs:975-982 | two of the ten player-facing presets the scaffolder wires (slot 2 `Blueprint_DefaultGrappler`, slot 8 `Blueprint_DefaultHoverTank`) are in neither `PresetPaths` nor ScriptedChassisBuilderTests' list, so no test validates them; the CHG-003 guard checks list→disk only, and its docstring overclaims | red team CHG-003 notes; GameplayScaffolder.cs:975-982 | add both to both lists, make the two lists one source, reword the docstring | S | AUTO→CHG-008 LANDED 2026-09-17 (docs/changes/178-preset-coverage.md)
F-023 | 2026-09-17 | inconsistency | S3 | docs/TRACES.md | the generated trace index is stale in 17 of its 70 indexed sites on main (16 before CHG-002, +1 after: it still lists `DOC:best-practices§statics` at HudPointerGuard.cs:32); it is regenerated only by Robogame → Traces → Rebuild Index, which needs the Editor (D-005) | red team CHG-002, 2026-09-17 (.utmp/factory/gate/CHG-002-redteam.md); docs/TRACES.md | rebuild the index once the Editor opens; add docs/TRACES.md to the doc-drift sweep's list | S | AUTO→CHG-014 LANDED 2026-09-17 (docs/changes/182-traces-index.md; the scanner's own gap is F-041)
F-024 | 2026-09-17 | best-practice | S3 | Assets/_Project/Scripts (9 sites) | the batch compile logs 18 `warning CS0618` lines (9 sites × 2 assemblies): obsolete `FindObjectsByType` / `FindObjectsSortMode` overloads in Tools/PerfBisect.cs:85-86,119-120,158-159 and an obsolete `FindFirstObjectByType` overload in Core/UiTween.cs:89, Core/MusicConductor.cs:94, Core/AudioRouter.cs:124 | .claude/worktrees/test-rig/TestRun-EditMode.log 2026-09-17T02:16Z (`grep 'warning CS'`; visible only when the assemblies recompile, a cached rerun logs 0) | migrate the nine calls to the non-obsolete overloads (I1 AUTO: a console warning fixed); the suite proves zero behaviour change | S | AUTO→CHG-009 → CLOSED 2026-09-17 shift 7 by CHG-009 (docs/changes/189)
F-025 | 2026-09-17 | inconsistency | S3 | ProjectSettings/ProjectSettings.asset:838 | the Standalone scripting-define line still carries `LETAI_TRUESHADOW`, auto-added by the TrueShadow editor script that CHG-005 deleted; no surviving .cs/.shader/.hlsl/.asmdef consumes the symbol, so it is inert litter that misleads the next provenance reader (`GRASSFLOW_SRP` on the same line is the same class: GrassFlow is disconnected per art-direction.md) | red team CHG-005, 2026-09-17 (.utmp/factory/gate/CHG-005-redteam.md); ProjectSettings/ProjectSettings.asset:838 | strip the dead defines from the Standalone line (ProjectSettings only; the suite proves nothing changes) | S | AUTO→CHG-010 LANDED 2026-09-17 (docs/changes/179-dead-defines.md)
F-026 | 2026-09-17 | bug | S2 | Assets/_Project/ScriptableObjects/Blueprints/Blueprint_DefaultHoverTank.asset (entries at (-2,0,-2), (1,0,-2), (-2,0,1), (1,0,1)) | the shipped HoverTank preset fails library-aware validation: four corner cubes at y=0 have Up=+Y, so their implied host is the hoverblade beneath (a leaf block that accepts no mount); the chassis builds and drives, but it is not player-buildable-shaped, the property the presets are meant to keep ("default presets are equivalent to player saves") | CHG-008's coverage, test rig 2026-09-17T15:55Z: `Preset_PassesValidation(Blueprint_DefaultHoverTank)` Failed with 4 host-face errors (quarantined in PresetBlueprintTests.KnownInvalid, which fails the day the fix lands) | set the four entries' Up so each is hosted by a neighbouring cube (e.g. Up=-X for x=-2, Up=+X for x=1), in the asset (no code writes it: GameplayScaffolder lists thirteen presets at :107-141 and only loads HoverTank at :951, so HoverTank alone never passed the write-time validator (red team, CHG-008); adding it to the scaffolder's preset list is a follow-up); a cube's orientation is invisible, so no visual change | S | AUTO→CHG-013 → RECLASSED ASK 2026-09-17 (red team: the cube's plank albedo is UV-mapped and follows Up, F-033); built and green on chg/013-hovertank-preset @ 259f0b03, NEEDS-GREY APPROVE
F-027 | 2026-09-17 | best-practice | S3 | the whole project on a full recompile | the complete compiler-warning census: 254 `warning CS` lines when every assembly rebuilds (CHG-010's define change forced it): ~240 CS0618 obsolete-API uses (FindFirstObjectByType / FindObjectsByType / FindObjectsSortMode overloads ≈ 220, GetInstanceID ≈ 18, UnityStats ≈ 4, IStyle ≈ 2), 6 CS0414 unused-field, 2 CS0219 unused-variable; by file: third-party MidiPlayer (TestLoadSF 16, MidiSynth 14, MidiPlayerGlobal 14, TestMidiGenerator 10) and Water.cs 12 are not ours; first-party: PerformanceHud 12, PerfRenderProbe 12, PerfBisect 12, RotorBlockTests 8, ScreenLogger 6 and the three Core sites in F-024 | .claude/worktrees/test-rig/TestRun-EditMode.log 2026-09-17T16:09Z after a full recompile (`grep -o 'warning CS[0-9]*' \| sort \| uniq -c`) | CHG-009 widens to every first-party CS0618/CS0414/CS0219 site (the compiler names the replacement); third-party warnings are a package-upgrade question (ASK) and are excluded; a `warning CS` count in the batch log becomes a suite-and-perf sweep row | M | AUTO→CHG-009 (first-party) / NOTE (third-party) → first-party CLOSED 2026-09-17 shift 7 by CHG-009 (docs/changes/189): 46 → 3 sites, the three named there; third-party stays NOTE
F-028 | 2026-09-17 | best-practice | S3 | Assets/_Project/Tests/EditMode/Provenance/ScriptingDefinesTests.cs | the define guard matches a substring in any source file, so a pack's define-SETTER script (e.g. PostProcessing's DefineSetter.cs, the only mention of UNITY_POST_PROCESSING_STACK_V2 anywhere) counts as a consumer; it catches a fully deleted pack (F-025's case) but not a pack whose setter survives while its `#if` readers are gone; it also checks Standalone only while CHG-010 edited seven platform lines | red team CHG-010, 2026-09-17 (.utmp/factory/gate/CHG-010-redteam.md notes 1, 3) | tighten to `#if`/`#elif` readers and asmdef versionDefines, excluding files that only write the symbol; check every platform line | S | AUTO→next touch of the test (fold into CHG-009 or its own change)
F-029 | 2026-09-17 | best-practice | S3 | Packages/manifest.json (com.unity.postprocessing 3.5.4) | the Post Processing Stack v2 package is installed in a URP project and nothing under Assets/ consumes it (URP has its own volume/post stack); its DefineSetter.cs re-injects UNITY_POST_PROCESSING_STACK_V2 on every domain reload | red team CHG-010, 2026-09-17, note 9; `grep -rl UNITY_POST_PROCESSING_STACK_V2` = only the package's own setter | remove the package if nothing uses it (a package removal is ASK under I1) or record why it stays | S | ASK→NEEDS-GREY when the board has room (a one-line question) → 2026-09-18 shift 8: re-checked: our PostProcessingBuilder/EnvironmentBuilder author URP VolumeProfiles (UnityEngine.Rendering.Universal), not PPv2; the PPv2 PostProcessVolume script (guid 8b9a305e18de0c04dbd257a21cd47087) is in no scene or prefab; only the package's own DefineSetter.cs names the define → ASK→D-008 (one line on the board)
F-030 | 2026-09-17 | readiness | S3 | the six shipped scenes (ProjectSettings/EditorBuildSettings.asset: Bootstrap, MainMenu, Garage, Arena, WaterArena, PlanetArena) | first console sweep over the live bridge: 0 errors and 0 warnings on the Editor console before any load and after each scene load (7 reads, `read_console` errors+warnings, count 200, detailed), Editor instance robogame-factory@0de2e8ec2dc2f03e on main @ 8751b11e; the compile-time warning census is F-027 and is not visible on a cached load | .utmp/factory/sweeps/console-2026-09-17.md (gitignored; rerun: the console sweep brief in docs/loop/LOOP-STATE.md § FACTORY FLOOR); LOOP-STATE shift 5 bullet | none; LAUNCH-READINESS T2 → done (in-Editor load only; a player build's log is B3) | — | NOTE
F-031 | 2026-09-17 | bug | S2 | the rig: .claude/scripts/run-tests.sh + the MCP for Unity package (Library/PackageCache/com.coplaydev.unity-mcp@f445f81ac937/Editor/Services/McpEditorShutdownCleanup.cs:50 → ServerManagementService.StopManagedLocalHttpServer) | every batch test run KILLS the factory Editor's MCP server: the package stores its "server I launched" handshake (pidfile path + token) in EditorPrefs (IPidFileManager.cs:45, per user, not per project), so the rig's batch Editor, on quit, reads the factory Editor's handshake and terminates the factory's server, then deletes the pidfile; the bridge is down for the rest of the session and the Editor's client only retries the socket | server log last line 12:54:26Z (…/UnityMCP/Logs/unity_mcp_server.log), Library/MCPForUnity/RunState emptied 12:54:33Z, the builder's step-1 batch run quit in that window; Editor.log: "remote party closed the WebSocket"; the batch logs show no MCP start/stop line (the stop is silent) | run-tests.sh strips com.coplaydev.unity-mcp from the RIG's Packages/manifest.json after the sync (no test references the package; grep 2026-09-17), and the loop restarts the server by hand when 8080 is silent (the command is in ServerCommandBuilder.TryBuildCommand); upstream: the handshake should be SessionState or project-scoped (report to CoplayDev) | S | AUTO→CHG-015
F-032 | 2026-09-17 | best-practice | S3 | the factory Editor console after a domain reload (Robogame.Tests.EditMode recompile, CHG-013 branch switch, ~12:54Z) | Unity Jobs leak detection: "Leak Detected : Persistent allocates 847 individual allocations" logged once after the reload; a Persistent NativeArray/NativeList not disposed before the domain reload, in editor or test code (INV-6 neighbourhood: allocations that outlive their owner) | `read_console` over the live bridge 2026-09-17T18:05Z, entry 4 of the last 10 (LOOP-STATE shift 5 bullet); the message names no stack until Preferences > Jobs > Leak Detection Level is "Enabled With Stack Trace" | SPIKE: set the leak detection level with stack traces in the factory Editor (`execute_code`: `UnityEngine.Unity.Collections.NativeLeakDetection.Mode = EnabledWithStackTrace`), trigger one recompile, read the stack, then file the owner as a bug with the line | S | SPIKE→SPIKES L6 NOTE (constant 847 at every reload, allocated at startup; a stack needs the pref set before launch)
F-033 | 2026-09-17 | visual | S2 | Assets/_Project/Scripts/Block/BlockGrid.cs:181 (`localRotation = OrientationFromUp(up, yawDeg)`) + Materials/Blocks/BlockMat_Structure.mat (`_ArtisticProjection: 0`, albedo Art/Textures/PlankOak_Inv.png, 1x1 tiling) | a structure cube's visible plank grain follows its Up: every cube a player mounts on a host's side face (Up=±X/±Z, the normal PlacementRules path) renders cross-grain against its upright neighbours, so any player build with side-mounted cubes shows mixed grain; the primitive cube is UV-mapped per face, so a 90° roll rotates the texture | red team CHG-013, 2026-09-17 (.utmp/factory/gate/CHG-013-redteam.md Q4, each link verified in the repo) | Grey's art domain (art-direction.md, style exploration mode): a world-space/triplanar projection (the shader's `_ArtisticProjection`) or a rotation-invariant albedo would make block orientation invisible; asked as part of the CHG-013 APPROVE entry, not as its own DECIDE | S | ASK→NEEDS-GREY (folded into APPROVE CHG-013)
F-034 | 2026-09-17 | best-practice | S3 | Assets/_Project/Tests/EditMode/Blueprints/PresetBlueprintTests.cs:152-157 and ScriptedChassisBuilderTests.cs:187-192 (branch chg/013-hovertank-preset) | the tightened KnownInvalid quarantine (expected error text per entry) is unexercised: the table is empty from the tests-first commit on, so a wrong expected string fails nothing and no test feeds a synthetic bad plan through the checker (CLAUDE.md Rule 9: a test that cannot fail is wrong) | red team CHG-013, 2026-09-17, Q2 | add one EditMode test that builds a synthetic plan with a known host-face error, quarantines it in a local table and asserts the checker (a) passes with the right text and (b) fails with a wrong one | S | AUTO→CHG-016 (after CHG-013 lands) → CLOSED 2026-09-17 shift 7 by CHG-016 (docs/changes/188)
F-035 | 2026-09-17 | inconsistency | S3 | docs/blueprint-snapshots/presets.md (legend) and the writer DumpAllPresets_WritesAsciiSnapshot (PresetBlueprintTests.cs) | the ASCII snapshot's legend has no entry for the `?` glyph (hoverblade) or the `X` glyph (Grappler), both printed in the Grappler and Hover Tank sections the regenerated file gains on chg/013-hovertank-preset | red team CHG-013, 2026-09-17, follow-up note | add the two glyphs to the legend the dump writes (test code + regenerated doc) | S | AUTO→CHG-016 (same file, after CHG-013 lands) → 2026-09-18 shift 8: still open (CHG-016 did not touch it): the legend at BlueprintAsciiDump.cs:99-101 lacks n=Magnet and X=GrappleMagnet, and HoverBlade has no glyph at all (the four Hover Tank `?` are the unmapped fallback) → AUTO→CHG-025 (legend derived from the map + an H glyph) → 2026-09-18 CHG-025 builder: the gap is wider than the finding drew it: six other shipped presets print `?` for five more unmapped ids (Cannon, Mortar, Drill, Spring, ModuleEmp; per-preset table in .utmp/factory/gate/CHG-025/RESULT.md); foreground arbitration: CHG-025 widens the map to every id the 14 presets use and the legend test guards the next new block
F-036 | 2026-09-17 | inconsistency | S2 | docs/changes/README.md (the session-log index; CLAUDE.md: "the highest-numbered file is the current state of WIP") | the index's "Sessions (newest first)" table stops at 131 (docs/changes/README.md:89), so 48 entries (132-179: Grey's sessions 132-173 and the factory's 174-179) are unlisted; `land.py land` writes docs/changes/NNN-slug.md, the LOOP-STATE bullet and the NEEDS-GREY line but never the index row, so the file CLAUDE.md points every session at keeps drifting | `grep -o '1[0-9][0-9]-[a-z0-9-]*\.md' docs/changes/README.md \| sort -n \| tail -1` = 169 (a prose mention), the table's top row is 131, on main @ 6fc4e9ce; land.py has no README reference | (a) backfill rows 132-179 from each entry's first heading (mechanical); (b) `land.py land` appends the index line as step 6b, with a unit test in .claude/scripts/factory/tests; I1 AUTO: doc drift against code (the changes index is named in the AUTO list) + an instrument | S | AUTO→CHG-017 LANDED 2026-09-17 (docs/changes/185; rows 132-186 present; the 108-122 gap is F-044)
F-037 | 2026-09-17 | best-practice | S2 | Assets/_Project/Scripts/Movement/GroundDriveSubsystem.cs (_acceleration:27, _maxSpeed:28, _turnRate:31) | three serialized tuning fields are assigned but never read (compiler CS0414 on a full recompile): either dead knobs left behind by the per-block movement migration (docs/changes/README.md § Known unknowns names ground/hover thrust as migrated) or a tuning path that silently stopped being wired, which is INV-1's neighbourhood (a knob that does nothing misleads the tuner) | .utmp/factory/sweeps/warnings-census-editor-2026-09-17.txt (forced CleanBuildCache recompile, factory Editor, 2026-09-17T18:32Z); the census is rerunnable | read the three fields' history (git log -S) and either delete them (an ASK if they are visible in the inspector/tuning UI) or wire them; fold into CHG-009 only if they are dead | S | SPIKE (a git -S read first; then AUTO→CHG-009 or ASK) → CLOSED 2026-09-17 shift 7 by CHG-021 (docs/changes/187): the fields were the stranded inline defaults from the blueprint-authoritative migration; deleted, no prefab or scene carried them
F-038 | 2026-09-17 | bug | S2 | Assets/_Project/Tests/PlayMode/Voxel/SurfaceNetsBenchmarkTests.cs (CHG-012's test) | the benchmark's post-recompile case survives CHG-012: the first full-suite run after the test assembly recompiled read medians 1.039/0.942/1.412 ms (best 0.942, 5.8 % from red; two windows over 1 ms), passing only through best-of-three, while the next run in the same session read 0.430 ms; the synchronous Burst compile in the warm-up covers a cold cache (3/3 measured) but not whatever runs alongside the first post-reload window (leading guess: Burst compiling other assemblies' jobs on worker threads) | red team CHG-012, 2026-09-17, note 1 (.utmp/factory/gate/CHG-012-redteam.md); the row was in the rig's harness-log before the sync destroyed it (its text is in the note) | SPIKE: reproduce by deleting the rig's Library/ScriptAssemblies before a full PlayMode run and reading the row 5×; if it reproduces, either drain Burst first (a bounded spin on a sentinel job until it runs under its warm time) or run the benchmark in its own PlayMode invocation (run-tests.sh gains a Perf leg) | S | SPIKE→SPIKES L7
F-039 | 2026-09-17 | inconsistency | S3 | Assets/_Project/Scripts/Voxel/SurfaceNetsMesher.cs:60 (doc comment "< 1 ms budget at dim=33") and docs/subsystems/terraforming.md:611 (after CHG-012: names the warm-up, the windows and the best-median gate but not the synchronous Burst compile that makes them work) | two remaining dim=33 / mechanism stragglers after CHG-012 renamed the test to Dim34 and fixed the terraforming line | red team CHG-012, 2026-09-17, notes 4 and 5 | one-line comment fix + one clause in terraforming.md; fold into the next doc-drift or CHG-009 touch | S | AUTO→next doc-drift change → AUTO→CHG-025 (2026-09-18)
F-040 | 2026-09-17 | inconsistency | S3 | Assets/_Project/Materials/Mat_ArenaStair.mat, Mat_GarageFloor.mat, Mat_GarageWall.mat, Mat_WaterFloor.mat (each: the `_Color` row under m_Colors) | the factory Editor rewrites these four materials on scene load / play (the perf band and the sweeps): the only change is float noise in `_Color` (0.24705882 → 0.24705878 etc.), i.e. a re-serialization of the same colour, so every shift that opens a scene leaves four orphan diffs that the landing chain refuses (D13) | `git diff --stat Assets/_Project/Materials/` after the 2026-09-17T18:45Z perf band: 4 files, 4 insertions, 4 deletions, all `_Color`; reverted with `git checkout --` | commit the Editor's own serialization once after proving a second load leaves no diff (open a scene, diff, repeat); if the value keeps flipping, find the writer (a script copying `_AlbedoColor` into `_Color` at load, or the MK Toon material upgrader) | S | AUTO→CHG-018 (a normalization, not visible) → shift 7 2026-09-17: NOT reproduced by scene load (Garage, Arena, WaterArena) nor by a 12 s Arena play over the bridge, tree clean after each; the trigger is elsewhere (harness run_tests route or the visual sweep's camera/material calls); CHG-018 parked, SPIKE stays → REPRODUCED 2026-09-17 shift 7 by the in-Editor PlayMode test run (the perf-band harness over `run_tests`): after seven runs Mat_GarageFloor/GarageWall/WaterFloor each carried one `_Color` row of float noise (ArenaStair not this time); so the trigger is the Editor's test runner, not a plain scene load or play. The Editor's serialization is captured on chg/018-material-serialization @ 6781e6d5; its acceptance (a further harness run leaves the tree clean) decides whether this is one-time re-serialization or a per-run drift from a script writing the shared material (the values move a few ulps DOWN each time, which smells like a colour-space round trip; if the tree dirties again, find the writer) → CLOSED 2026-09-17 shift 7 by CHG-018 (docs/changes/192): one harness run after the landing left the three materials clean, so it was a one-time re-serialization
F-041 | 2026-09-17 | bug | S2 | Assets/_Project/Scripts/Tools/Editor/ContinualTraces.cs (the Scan regex) | the trace scanner only matches `TRACE[` immediately after `//`: of 131 raw `TRACE[` lines under Assets, 111 are indexed; 6 are the scanner's own self-exclusion, 3 sit on `///` doc lines and 11 sit mid-comment, among them BuildModeController.cs:92, the ONLY INV-2 anchor, so `## INV-2` never appears in docs/TRACES.md and a dangling INV-2 trace would never be reported | red team CHG-014, 2026-09-17, Q1 + note 1 (.utmp/factory/gate/CHG-014-redteam.md; its Python port of Scan reproduces the 111) | widen the match to `TRACE[` anywhere inside a `//` or `///` comment (not inside strings), keep the self-exclusion, add an EditMode test that plants a mid-comment trace in a fixture string and expects it scanned; rebuild the index once more | S | AUTO→CHG-019 LANDED 2026-09-17 (docs/changes/184; 125 traces, ## INV-2 present)
F-042 | 2026-09-17 | best-practice | S3 | .claude/agents/sweeper.md (the doc-drift step CHG-014 added, and the closing "What you DON'T do" section) | the new Rebuild-Index step has no bridge-down rule (a failed `execute_menu_item` leaves an empty diff and NO finding: a silent false negative, where the console sweep says BRIDGE_DOWN), the closing section still says the sweeper is read-only while the step writes and restores docs/TRACES.md, and the step assumes the live Editor's project root is the sweeper's git root (an Editor on the test-rig worktree would make the diff silently empty) | red team CHG-014, 2026-09-17, notes 2-4 | three sentences: BRIDGE_DOWN when the menu item fails; the closing section names the one write-and-restore; the step checks `Application.dataPath` via the instances resource matches the clone before trusting the diff | S | AUTO→CHG-019 LANDED 2026-09-17 (docs/changes/184)
F-043 | 2026-09-17 | bug | S3 | .claude/scripts/factory/ping.py (stdout printing) | the shift-5 report crashed before posting: printing a bullet with `≈` to the Windows cp1252 console raised UnicodeEncodeError, so the Discord post never happened and the ledger stayed empty (D12: one report per shift) | the traceback in the shift-5 end-of-shift Bash (loop-tick 18:58Z); `sent_2026-09-17.jsonl` last written 11:28Z | `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` at import (done in this commit; factory tooling, no product code); resend with PYTHONIOENCODING=utf-8 | S | AUTO→fixed 2026-09-17 (factory: ping.py UTF-8)
F-044 | 2026-09-17 | inconsistency | S3 | docs/changes/README.md § Sessions (newest first), rows 108-122 | fifteen session entries (docs/changes/108-*.md … 122-*.md) exist on disk but were never added to the hand-written table, a gap BELOW the table's top row that predates the factory; CHG-017's backfill is forward-only (rows above the current top), so the gap stays | the CHG-017 builder's RESULT.md (.utmp/factory/gate/CHG-017/RESULT.md), 2026-09-17; `ls docs/changes/1[01][0-9]-*.md` vs the table | extend backfill_changes_index.py with an `--all` mode that inserts any missing NNN in descending order among the existing rows; run once; land as a doc fix | S | AUTO→next touch of the index tooling (CHG-017b) → AUTO→CHG-025 (2026-09-18: `--all` mode; 19 rows: 101-104 and 108-122)
F-045 | 2026-09-17 | best-practice | S3 | .claude/scripts/factory/Start-Factory.ps1 (the shift lock it writes: `.utmp/factory/shift.json`, `pid: null` in -Desktop mode) | a Desktop shift's lock never gets a pid (the loop runs inside the Desktop app, not a child process), so every later preflight judges the lock "stale (pid gone)" after its grace period and absorbs a live shift's work as a predecessor's; harmless while one shift runs at a time, wrong the day two overlap | the peer session's CHG-020 build notes, 2026-09-17; shift 5's preflight printed exactly that warning about shift 4's lock | -Desktop mode writes the Desktop session id (or the launcher's own start time + a heartbeat the loop stamps in the tick) and the preflight judges staleness by the tick's age, not a pid | S | AUTO→next launcher touch (with CHG-020's follow-ups)
F-046 | 2026-09-17 | best-practice | S3 | Assets/_Project/Scripts/Gameplay/ArenaController.cs | dev-spawn triplication: SpawnTankDummy (1158-1204) and SpawnAirDummy (1321-1357) share the same ~35-line shape (liveness guard, blueprint resolve, stale-GameObject scrub, new GameObject + SetActive(false), attach bot AI + patrol params, ChassisFactory.Build, SetActive(true), ApplyFire, RegisterChassis, Debug.Log) differing only in field names and AI component type; DespawnTankDummy (1148-1156) and DespawnAirDummy (1311-1319) are the same null-guard/Destroy/null-out shape; SpawnStressTower (1094-1128) is a third near-copy of the Spawn shape | Assets/_Project/Scripts/Gameplay/ArenaController.cs:1158-1204 vs :1321-1357 (Spawn); :1148-1156 vs :1311-1319 (Despawn) | extract a SpawnDevDummy helper (ref GameObject go, ref TAi ai, name, spawn pos, blueprint, MatchSide, label, patrol-config callback) and a matching DespawnDevDummy in ArenaController or a new DevDummySpawner; call it from the three Apply*State entry points; deletes roughly 140 of the file's 1515 lines | M | AUTO (suite has MatchFlowTests + dummy coverage; behaviour-identical extraction) | → CLOSED 2026-09-17 shift 7 by CHG-022 (docs/changes/191); the line count did not fall (+9), the copies did (3 → 1)
F-047 | 2026-09-17 | best-practice | S3 | Assets/_Project/Scripts/Gameplay/ArenaController.cs (1515 lines) | god class: one MonoBehaviour owns match lifecycle (Start/Update/CreateMatch/HandleMatchStarted/HandleMatchEnded), player respawn, damage/scrap/score event handling, scrap-depot spawning, bot-target binding, stress-tower dev-dummy lifecycle, tank dev-dummy lifecycle plus fire-AI wiring, friendly-tank spawn, air dev-dummy lifecycle plus fire-AI wiring, and follow-camera binding - 9 distinct responsibilities in one file, none of which need each other's private state to work | Assets/_Project/Scripts/Gameplay/ArenaController.cs:1066-1393 (stress-tower/tank/air-dummy/friendly-tank block, about 330 of the file's 1515 lines) is entirely the dev/test-bot concern, separable from match orchestration (:210-767) | split the dev-dummy block (stress tower, tank dummy, friendly tank, air dummy - :1066-1393) into a new ArenaDevDummies component ArenaController composes/delegates to, leaving ArenaController with match lifecycle and scoring only; about 330-line extraction | L | ASK (a MonoBehaviour split touches serialized-field wiring on the Arena/WaterArena/PlanetArena scenes - inspector references move, not proven zero-risk by the suite alone) | → foreground 2026-09-17: under I1's calibration a split of DEV-FACING code is AUTO once F-046 lands and the scene wiring is verified over the bridge (scene loads + play on Arena/WaterArena/PlanetArena); re-class AUTO→CHG after CHG-022 → 2026-09-18 shift 8: AUTO→CHG-024 (specced; the boundary narrowed: the FRIENDLY TANK (:101-118, :1271-1328, :859-873) is a shipped Phase 1 feature spawned by default and stays in ArenaController; ArenaController is in Arena.unity only, GUID grep 2026-09-18, so one scene moves)
F-048 | 2026-09-17 | perf | S3 | Assets/_Project/Tests/PlayMode/Perf/PerfRenderProbe.cs (the shadow-caster window) | the probe captures the chassis' block MeshRenderers at the start and toggles their shadow casting two measurement windows later; by then some are destroyed (the chassis' instanced renderer consolidates them), so the 'casters off' window measures fewer casters than captured and the attribution row is optimistic by that share; the MissingReferenceException it threw once is fixed (CHG-023) but the measurement question stands | .utmp/factory/gate/CHG-018-suite.txt (first attempt; stack at PerfRenderProbe.cs:131); CHG-023's new log line `[RENDER-PROBE] shadow casters toggled: N of M` in every later batch log | read N of M from the next PlayMode log; if N < M by more than a few, re-capture the renderers right before the toggle (or toggle on the instanced renderer) and re-baseline the render-probe row | S | SPIKE (one log read) then AUTO → 2026-09-18 shift 8: NOTE, read from the rung-0 PlayMode log: `[RENDER-PROBE] shadow casters toggled: 666 of 666 captured renderers still alive`, so the attribution row is not optimistic on this rig; re-open if N < M ever prints
F-049 | 2026-09-18 | best-practice | S2 | Assets/_Project/Scripts/Gameplay/BlockEditor.cs:977-1137 | GOD CLASS: the instance-edit + tune-mode hover highlight subsystem (ClearInstanceEdit, HighlightInstance, FitShellToBlock, DriveHoverHighlight, PulseHoverHighlight, HideHoverHighlight, ~161 lines) is self-contained — it only needs _grid.CellSize and its own highlight GameObjects/materials, none of the targeting/placement state the rest of the 1252-line class owns | Assets/_Project/Scripts/Gameplay/BlockEditor.cs:977-1137 | extract to a small BlockEditHighlights helper class/component owned by BlockEditor (~160 lines moved, no behavior change) | M | AUTO→CHG (S1 extraction; after CHG-027)
F-050 | 2026-09-18 | best-practice | S3 | Assets/_Project/Scripts/Gameplay/BlockEditor.cs:1008-1022,1098-1113 | DUPLICATION: HighlightInstance and DriveHoverHighlight's inline block both build a highlight primitive the same way (CreatePrimitive(Cube), destroy the Collider, assign MeshRenderer.sharedMaterial, disable shadow casting/receiving) — ~15 near-identical lines twice | Assets/_Project/Scripts/Gameplay/BlockEditor.cs:1008-1022 vs Assets/_Project/Scripts/Gameplay/BlockEditor.cs:1098-1113 | factor out a MakeHighlightShell(Material) helper used by both call sites (~15 lines removed) | S | AUTO→CHG-027 (2026-09-18: bundled with F-051, F-054: three construction de-duplications)
F-051 | 2026-09-18 | best-practice | S3 | Assets/_Project/Scripts/Gameplay/SettingsHud.cs:427-445,483-501,668-681,734-747 | DUPLICATION: AddActionRow, AddBisectToggleRow, AddSliderRow and AddBoolRow each rebuild an identical ~20-line row shell (NewChild row + LayoutElement height 44 + tinted Image background + left-aligned label Text) before diverging into their control-specific widget | Assets/_Project/Scripts/Gameplay/SettingsHud.cs:427-445 (AddActionRow) vs :483-501 (AddBisectToggleRow) vs :668-681 (AddSliderRow) vs :734-747 (AddBoolRow) | extract a shared BuildRowShell(string name, string label) helper returning (rowGO, labelText) (~60 lines removed across 4 call sites) | M | AUTO→CHG-027 (2026-09-18)
F-052 | 2026-09-18 | best-practice | S3 | Assets/_Project/Scripts/Gameplay/SettingsHud.cs:955-1035 | GOD CLASS: BuildKeybindsSection + AddKeybindRow (~80 lines) is a static reference table with stub Tweakables.Spec entries just to ride the search/foldout pipeline — it needs only _content and the group-header helpers, none of the Tweakable-row-building responsibility the rest of the class owns | Assets/_Project/Scripts/Gameplay/SettingsHud.cs:955-1035 | extract to a KeybindsSectionBuilder (or fold the static keybind list into a ScriptableObject/data table read by a smaller builder) (~80 lines moved) | S | AUTO→CHG (S1 extraction; queue after CHG-027)
F-053 | 2026-09-18 | best-practice | S2 | Assets/_Project/Scripts/Gameplay/LabController.cs:580-1177 | GOD CLASS: one-shot decorative UGUI scene construction (BuildGround/PlaceBlotch/BuildFogBank/BuildScrews/BuildScrew/BuildHeader/BuildJournal/BuildSwitchboard/BuildSliderRow/BuildNameField/BuildActions/BuildVial) is ~600 of the file's 1207 lines — half the class — and is pure layout/visual scaffolding with zero Concoction-editing logic; it needs only _root plus the widget refs it returns/assigns, not the editor-state fields (_dmg/_size/_kb/_speed/_spread/_editingId) | Assets/_Project/Scripts/Gameplay/LabController.cs:580-1177 | extract to a LabCanvasBuilder that constructs the canvas and hands back the wired widget references; LabController keeps only the Concoction editor logic + Update animation (~600 lines moved, no visual change) | L | AUTO→CHG (S1 extraction, L; queue; an ASK if any pixel moves: the Lab canvas is on the player's path, so the extraction must be proven pixel-identical by a screenshot diff over the bridge)
F-054 | 2026-09-18 | best-practice | S3 | Assets/_Project/Scripts/Gameplay/LabController.cs:462-469,524-530,743-749,1010-1016 | DUPLICATION: four call sites hand-roll the same Button + ColorBlock styling pattern (AddComponent<Button>(); read .colors; set normal/highlighted/pressed; write .colors back) — row-select button, New-row button, header Close button, Save button | Assets/_Project/Scripts/Gameplay/LabController.cs:462-469 (row select) vs :524-530 (New row) vs :743-749 (Close) vs :1010-1016 (Save) | extract a StyleButton(Button, normal, hover, pressed) helper (~25 lines removed across 4 sites) | S | AUTO→CHG-027 (2026-09-18)
F-055 | 2026-09-18 | best-practice | S3 | .claude/scripts/factory/land.py (step 4, the bullet length) | `land.py land` prints the LOOP-STATE bullet length ("bullet 707 chars") and proceeds; charter D1 and the LOOP-STATE § SHIFT LOG header say the chain REFUSES a bullet over 600 characters, so the cap is a wish, not a check (D13: "a budget the chain does not check is a wish") | the CHG-026 landing 2026-09-18T05:13Z: `[4] record: docs/changes/193-perf-band-focus.md · bullet 707 chars` then `[5] merged` | refuse > 600 in `land` (and `--dry-run`), with a unit test; shorten this shift's 707-char bullet is not needed (the record stands) | S | AUTO→next land.py touch
F-056 | 2026-09-18 | inconsistency | S3 | docs/changes/README.md (the row for 109, once CHG-025's `--all` backfill lands) and .claude/scripts/factory/backfill_changes_index.py (`title_from_heading`) | the backfill reads an entry's FIRST LINE as its title; docs/changes/109-full-app-code-review.md opens with an HTML comment (`<!-- Generated by a multi-agent full-app audit… -->`), not a `# 109 — Title` heading, so its index row carries the comment text as its title; every other entry 100–193 starts with a heading | the CHG-025 builder's RESULT.md (.utmp/factory/gate/CHG-025/RESULT.md § (c)); `head -1 docs/changes/109-full-app-code-review.md` | `title_from_heading` skips leading HTML comments and blank lines to the first `#` heading (one unit test); rerun `--all` (idempotent: only that row's text changes) or hand-fix the one row | S | AUTO→next index-tooling touch (with F-055's land.py fix)
