# CHANGE-QUEUE — specs awaiting build (D4 CHANGE tier), ranked by pillar value × cheapness

## ENGINEERING RULES (binding on every change; the product earns these — promote from LESSONS.md § METHOD LESSONS when they recur)

1. The ten invariants in docs/invariants.md, by number, are rules here too; a change that touches one names it in "Must not break".
2. Profile before claiming a perf characteristic (INV-7): a Profiler capture or a harness row, on this rig, with its band.
3. A bug found is a test written; a flaky test is a bug.
4. Every new feature ships with VFX + audio (INV-8); a missing cue is declared, not skipped.
5. Fieldhands edit DISJOINT files in the factory clone; the foreground commits named paths; nothing is ever edited under `.claude/worktrees/` (the Editor watches the clone root; the PreToolUse hook enforces it).
6. The test-rig worktree is never opened in an Editor; `run-tests.sh` clobbers it on every run.
7. The batch rig (`-nographics`) and the live Editor are different rigs; their numbers are not compared.
8. Load-bearing lines whose reason is not obvious carry a `// TRACE[id]: note` (CLAUDE.md); a change that cites an ADR, an invariant or a session log anchors it.
9. One docs/changes entry per landed change, written by `land.py` from the change's evidence; never hand-appended to an older entry.
10. Features come through the idea backlog: /ideate's `rejected` is never re-pitched; a new feature the loop conceives enters the backlog as `proposed` before it enters this queue.
11. The I1 class is written on the spec; an ASK-class change waits on the board and never starts BUILD before its check-off.

## SPEC FORMAT (one page, written BEFORE code)

    ### CHG-NNN title — status: SPEC / APPROVE-WAIT / TESTS / BUILD / GATE / LANDED
    Class: AUTO | ASK (I1)
    Pillar or readiness item: which, and how this serves it
    Source: F-NNN / SPIKES L<n> / backlog item / NEEDS-GREY verdict
    Change: what, in one paragraph
    Acceptance: a test name, a proxy delta with its band, or ONE playtest question — never "is it fun"
    Must not break: invariants and subsystems touched
    Revert: how; what state it leaves
    Feel change? yes → a PLAY entry after landing; no

## QUEUE

| rank | id | title | class | pillar / readiness | acceptance kind | est. cost | status |
|---|---|---|---|---|---|---|---|
| 1 | CHG-003 | PresetBlueprintTests: drop the stale DefaultBuggy path | AUTO | T1 | test (zero Inconclusive) | S | LANDED 2026-09-17 (docs/changes/174) |
| 5 | CHG-008 | preset test coverage: Grappler + HoverTank, one list | AUTO | T1 | test (14 cases pass; slot-coverage guard) | S | LANDED 2026-09-17 (docs/changes/178-preset-coverage.md) |
| 7 | CHG-010 | strip the dead scripting defines (LETAI_TRUESHADOW, GRASSFLOW_SRP) | AUTO | L1 (F-025) | test (every Standalone define has a consumer) | S | LANDED 2026-09-17 (docs/changes/179-dead-defines.md) |
| 8 | CHG-013 | HoverTank preset: corner cubes hosted on cubes (F-026) | ASK (last of its kind: I1 now makes dev-facing content AUTO) | T1 | test (quarantine empties, 14/14 pass) | S | LANDED 2026-09-17 (docs/changes/183; Grey approved and narrowed I1 + D16b in the same word) |
| 11 | CHG-016 | prove the KnownInvalid quarantine pattern fires (a synthetic bad plan + expected error text) | AUTO | T1 (coverage) | test (a wrong expected string fails) | S | queued (F-034; after CHG-013 lands, so the two do not collide in PresetBlueprintTests.cs) |
| 12 | CHG-017 | docs/changes/README.md sessions table: backfill 132-179 from the entries' headings; land.py writes the row from now on (F-036) | AUTO | docs current (D13; CLAUDE.md § Active work points at this index) | test (land.py's unit test asserts the row; the table's top row = the highest file) | S | LANDED 2026-09-17 (docs/changes/185-changes-index.md; the older 108-122 gap is F-044) |
| 9 | CHG-012 | SurfaceNets benchmark: warm the job synchronously, best-of-three medians, a harness row (F-021) | AUTO | T1 | reproduce cold, then 3 cold + 2 warm runs pass and log rows | S | LANDED 2026-09-17 (docs/changes/181-surfacenets-gate.md; residual F-038) |
| 10 | CHG-014 | docs/TRACES.md rebuilt from the Editor; the doc-drift sweep checks it (F-023) | AUTO | docs current (D13) | a second Rebuild Index on the same sha is a no-op; Validate 0 dangling | S | LANDED 2026-09-17 (docs/changes/182-traces-index.md; follow-ups F-041, F-042 → CHG-019) |
| 13 | CHG-018 | commit the Editor's own serialization of four materials once (F-040: float noise in `_Color` on every scene load) | AUTO | the rig / D13 (a clean tree for every landing) | a second scene load leaves `git status` clean | S | queued |
| 14 | CHG-019 | ContinualTraces: scan mid-comment traces (F-041, the only INV-2 anchor is unindexed) + the sweep brief's bridge-down and read-only rules (F-042) | AUTO | docs current (D13); INV-2 traceability | EditMode test: a planted mid-comment trace is scanned; index rebuilt; the brief says BRIDGE_DOWN | S | LANDED 2026-09-17 (docs/changes/184-traces-scan.md; 111 → 125 traces, INV-2 anchored) |
| 0 | CHG-020 | server-first bridge: the launcher starts the MCP server by hand when 8080 is silent, waits, then the Editor (foregrounded once), then prints the prompt; `mcp_http.py` drives the bridge over plain HTTP when the session's connector is dead; /robogame-factory 1c uses it (BACKLOG 12, ASSUMPTIONS #8/#12, F-031) | AUTO (tooling; red team N/A, D16b) | the rig (D7): the bridge is the console, screenshot and profiler instrument, and it has been lost by timing in shifts 3, 4 and 6 | factory unit tests + a dry-run of the launcher's ordering; suite green | S | LANDED 2026-09-17 (docs/changes/186-server-first-bridge.md; real test = the next shift's start; the login task is D-007) |
| 0 | CHG-015 | the test rig must not kill the factory's MCP server (F-031): run-tests.sh strips the MCP package from the rig | AUTO | the rig (D7: the bridge is the console, screenshot and profiler instrument) | reproduce (8080 dies at a batch quit), then 3 runs leave 8080 listening; suite counts unchanged | S | LANDED 2026-09-17 (docs/changes/180-rig-strips-mcp.md) |
| 6 | CHG-011 | rig audio mute (Grey's steer 2026-09-17) | AUTO | rig (D7) | test (batch session is muted) | S | PARK on PT-001 (branch chg/011-rig-audio-mute @ abf6de35; the test could not fail first: a batch Editor already reports the mute on) |
| 2 | CHG-002 | doc drift from the 2026-09-16 sweep (six edits) | AUTO | D1, D2 | sweep re-run + Traces Validate | S | LANDED 2026-09-17 (docs/changes/175-doc-drift-sweep.md; red team KILL then PASS) |
| 3 | CHG-001 | provenance records: artgen manifest + unity-mcp package row + three Asset Store rows (D-004) | AUTO | L1, L2 | test (manifest covers every FBX) + provenance re-sweep | S | LANDED 2026-09-17 (docs/changes/177-provenance-records.md) |
| 4 | CHG-005 | delete the two unused packs (FattyPolyTurretFree + Part2Free, Le Tai's TrueShadow) | ASK, nod given (D-004) | L1 | grep of their GUIDs in Assets/_Project = 0 + suite green | S | LANDED 2026-09-17 (docs/changes/176-delete-unused-packs.md) |

Specs pending (not yet written): CHG-009 first-party compiler warnings (F-024 + F-027; the full census with file, line and text is in `.utmp/factory/sweeps/warnings-census-editor-2026-09-17.txt` from a forced CleanBuildCache recompile in the factory Editor, 2026-09-17T18:32Z: 127 unique sites project-wide, 46 first-party in 19 files: PerfBisect 6, PerformanceHud 6, PerfRenderProbe 6, RotorBlockTests 4, GroundDriveSubsystem 3 (CS0414, F-037), GarageDecor 2, NameplateOverlay 2, ScrapCarriedIndicator 2, VoxelChaserBot 2, NetworkSceneFlow 2, PerformanceMenu 2, DigZoneTests 2, and one each in AudioRouter, MusicConductor, UiTween, ChassisInstancedRenderer, ObjectiveHud, SceneTransitionHud, GameplayScaffolder; by text: FindObjectsByType/FindObjectsSortMode overloads 34, FindFirstObjectByType 5, UnityStats.frameTime/renderTime 2, GetInstanceID 1, unused fields/variables 4. CAVEAT for the spec: `FindFirstObjectByType` → `FindAnyObjectByType` changes which instance is returned when more than one exists; only single-instance types migrate blind, the rest need a read. Acceptance: 0 first-party `warning CS` lines on a forced full recompile (the census procedure, rerunnable), suite green; cost M) · CHG-004 bomb-bay door cue (F-008, ASK: audible + visible; needs a read of the AudioCue / VfxKind enums first) · CHG-006 atomic blueprint and concoction writes (BACKLOG 4; UserBlueprintLibrary.cs:147, ConcoctionLibrary.cs:122, Tweakables.cs:512 write with File.WriteAllText) · CHG-007 enable MatchFlowTests.SpawnBot via a MinimalArena test scene (BACKLOG 2).

### CHG-003 PresetBlueprintTests: drop the stale DefaultBuggy path — status: LANDED (docs/changes/174, 2026-09-17; red team PASS with notes → F-021, F-022)
Class: AUTO (I1: a failing or flaky test fixed; the case is Inconclusive on every run)
Pillar or readiness item: LAUNCH-READINESS T1 (every inconclusive justified) — removes the one unjustified inconclusive from the suite.
Source: F-016; ASSUMPTIONS #10 (falsified).
Change: remove `BlueprintFolder + "/Blueprint_DefaultBuggy.asset"` from `PresetPaths` in Assets/_Project/Tests/EditMode/Blueprints/PresetBlueprintTests.cs:37. GameplayScaffolder.cs:38-39 records that session 61 retired the Buggy preset and none is in the repo, so the entry tests nothing; Assets/_Project/Tests/EditMode/Blueprints/ScriptedChassisBuilderTests.cs:177 names the same path and gets the same treatment. If a Buggy preset is meant to exist, that is a visible content addition and an ASK, not this change.
Acceptance: `PresetBlueprintTests.Preset_PassesValidation` reports 0 Inconclusive on the test rig (EditMode total drops by one; passed = total). Written first: a guard test `PresetPaths_AllExistOnDisk` that fails on main today because the Buggy path is absent, and passes after the edit.
Must not break: the presets already listed stay covered (the remaining twelve paths all exist on disk, 2026-09-17). Red team note: two scaffolder presets were never in the list (F-022 → CHG-008); this change neither adds nor removes coverage for them. No runtime code touched; no invariant.
Revert: `git revert` of the landing commit; leaves T1 as it is today.
Feel change? no

### CHG-002 doc drift from the 2026-09-16 sweep (six edits) — status: LANDED (docs/changes/175-doc-drift-sweep.md, 2026-09-17; red team round 1 KILL: tip-blocks.md:140 still said 250; fixed, round 2 PASS)
Class: AUTO (I1: doc drift against code, tier-2 docs, dangling TRACEs)
Pillar or readiness item: LAUNCH-READINESS D1 (README current) and D2 (architecture.md matches the code); best-practices § 12.5 currency.
Source: F-009, F-010, F-011, F-013, F-014, F-015.
Change: (a) HudPointerGuard.cs:32 trace `DOC:best-practices§statics` → `DOC:CLAUDE.md§Known failure modes`; (b) tip-blocks.md:16,31 leash damper 250 → 0, deliberate, citing RopeBlock.cs:597-624; (c) scalable-parts.md § Status → the shipped phases 0/1/1.5 with pointers to docs/changes/38 and 39; (d) architecture.md:53 `PlanetGravity` → `GravityField`; (e) README.md:254-262,276 date + Changelog pointing at docs/changes/README.md; (f) best-practices.md:573-574,778-780 strike the DevHud straggler note. One intent: drift found by one sweep; no behaviour.
Acceptance: Robogame → Traces → Validate reports zero dangling traces (bridge up), or the doc-drift sweep's file-level TRACE grep finds none (bridge down); a doc-drift re-sweep reports none of F-009–F-015. No unit test: prose and one comment.
Must not break: (a) edits a comment in a C# file, so the suite stays green (EditMode 529/530 → per CHG-003, PlayMode 152/153). Tier-2 docs stay consistent with docs/invariants.md.
Revert: `git revert`; leaves the six drifts as they are today.
Feel change? no

### CHG-001 provenance records: artgen manifest + unity-mcp package row — status: LANDED (docs/changes/177-provenance-records.md, 2026-09-17)
Class: AUTO (I1: provenance records, I6)
Pillar or readiness item: LAUNCH-READINESS L1 (generated assets record their generator) and L2 (third-party package rows).
Source: F-006, F-007.
Change: add `artgen/README.md` with a table mapping every FBX under Assets/_Project/Art/Models/** to its generator script in artgen/ (source of truth per docs/changes/130; FBX is a build artifact) plus the Blender version used; add a row to docs/PACKAGE_MODIFICATIONS.md for `com.coplaydev.unity-mcp` (origin: the GitHub pin in Packages/manifest.json:3; editor-only, non-shipping; its license as the package's own file states it); add three rows to docs/subsystems/art-direction.md § Imported Assets for Stylized Nature Pack (ArenaProps.cs:57), Polytope Studio trees (ArenaProps.cs:276) and Handpainted Grass and Ground Textures (FluffGround.cs:275): source Unity Asset Store, license the Unity Asset Store EULA, per Grey's D-004 line of 2026-09-17 quoted in NEEDS-GREY § ANSWERED.
Acceptance: an EditMode test `ArtgenManifestTests.EveryGeneratedFbxHasAManifestRow` that parses artgen/README.md's table and asserts every .fbx under Assets/_Project/Art/Models has a row and every row's script exists on disk (written first; fails on main today: no manifest); and a provenance re-sweep that reports none of F-001, F-002, F-003, F-006, F-007.
Must not break: nothing at runtime; no scene, asset or import setting touched. INV-8 n/a.
Revert: `git revert`; leaves L1/L2 as they are today.
Feel change? no

### CHG-005 delete the two unused imported packs — status: LANDED (docs/changes/176-delete-unused-packs.md, 2026-09-17)
Class: ASK (I1: deleting assets needs Grey's nod). NOD RECORDED: Grey, 2026-09-17T01:52:02Z via /inbox, "FattyPolyTurretFree + Part2Free and Le Tai's TrueShadow: Delete both." (NEEDS-GREY § ANSWERED D-004).
Pillar or readiness item: LAUNCH-READINESS L1 (I6: a product that ships with an unlicensed asset is unshippable; these two have no license on disk and no use).
Source: F-004, F-005; D-004.
Change: `git rm -r` Assets/FattyPolyTurretFree, Assets/FattyPolyTurretPart2Free and "Assets/Le Tai's Asset/TrueShadow" (and their .meta files; if "Le Tai's Asset" then holds nothing else, the folder too). Nothing under Assets/_Project references them by script (provenance sweep 2026-09-16).
Acceptance: (1) before deleting, collect every GUID from the packs' .meta files and grep Assets/_Project (scenes, prefabs, materials, assets) for them: 0 hits, recorded in the docs/changes entry; (2) the suite stays green (EditMode 530/530, PlayMode 152/153); (3) the packs are gone from the tree.
Must not break: any scene, prefab or material that referenced a pack asset by GUID (the pre-check above is the guard; a hit turns this into a DROP or an ASK). No invariant touched; no runtime code.
Revert: `git revert` of the merge restores both packs byte-for-byte.
Feel change? no (nothing in a shipped scene references them, per the pre-check)

### CHG-008 preset test coverage: Grappler + HoverTank, one list — status: LANDED (docs/changes/178-preset-coverage.md, 2026-09-17)
Class: AUTO (I1: new test coverage)
Pillar or readiness item: LAUNCH-READINESS T1 — every shipped preset validated by the suite.
Source: F-022 (red team on CHG-003, 2026-09-17).
Change: add `Blueprint_DefaultGrappler.asset` (scaffolder slot 2, the preset that replaced Buggy) and `Blueprint_DefaultHoverTank.asset` (slot 8) to `PresetBlueprintTests.PresetPaths`; make `ScriptedChassisBuilderTests.EveryShippedPreset_PassesLibraryAwareValidation` iterate the same list (an `internal static` on PresetBlueprintTests, same asmdef) so the two lists cannot diverge; reword the CHG-003 guard's docstring to "list → disk" only.
Acceptance: `Preset_PassesValidation` runs 14 cases, all Passed; a new test `PresetPaths_CoverEveryScaffolderSlot` compares `PresetPaths` against the ten slot paths of GameplayScaffolder.cs:975-982 (hard-coded in the test with a comment naming that line, since the scaffolder's constants live in an Editor asmdef) — written first, fails on main today (Grappler, HoverTank missing).
Must not break: nothing at runtime; the guard from CHG-003 keeps passing.
Revert: `git revert`; back to twelve validated presets.
Feel change? no

### CHG-011 rig audio mute: no game audio from batch or factory Editors — status: PARK (PT-001; SPIKES L4: the acceptance test passes on main, so it proves nothing; the music's source is unmeasured)
Class: AUTO (I1: tooling, harnesses and instruments; not visible to a player, never runs in a build)
Pillar or readiness item: the rig (D7): Grey's tolerance of the factory is the binding constraint; the batch PlayMode runs played the garage music through the desktop speakers every two minutes (Grey, 2026-09-17T02:33Z, INBOX).
Source: INBOX 2026-09-17T02:33:13Z (Grey's steer).
Change: `Assets/_Project/Scripts/Tools/Editor/RigAudioMute.cs`, an `[InitializeOnLoad]` editor class that sets `EditorUtility.audioMasterMute = true` when `Application.isBatchMode` or the env var `ROBOGAME_RIG_MUTE` is `1`; `Start-Factory.ps1` sets that env var for the Editor it starts. The FMOD integration (RuntimeManager.cs:1513) and MusicConductor (:256) already mirror the Editor mute onto their buses, so one switch covers MPTK music, FMOD SFX and the bank-less music group. A human's Editor (no env var, not batch) is untouched.
Acceptance: EditMode test `RigAudioMuteTests.BatchMode_HasEditorAudioMuted`: in a batch session `EditorUtility.audioMasterMute` must be true (ignored in a human's Editor). Written first; fails on the rig today. Plus Grey's word that the speakers stay quiet during rig runs (FYI asks for it).
Must not break: nothing at runtime (editor-only assembly, Robogame.Tools.Editor); the suite's audio tests must still pass with the mute on (they assert routing, not audible output).
Revert: `git revert`; the music comes back.
Feel change? no (rig only)

### CHG-010 strip the dead scripting defines — status: LANDED (docs/changes/179-dead-defines.md, 2026-09-17)
Class: AUTO (I1: provenance litter / doc drift against code; the symbols have no consumer, so nothing compiles differently)
Pillar or readiness item: LAUNCH-READINESS L1 (F-025: a define left behind by a deleted pack misleads the provenance reader).
Source: F-025 (red team on CHG-005).
Change: remove `LETAI_TRUESHADOW` and `GRASSFLOW_SRP` from every platform's scripting-define line in ProjectSettings/ProjectSettings.asset (Standalone carries both; Android and Switch carry GRASSFLOW_SRP). `UNITY_POST_PROCESSING_STACK_V2` stays (the installed package's asmdef declares it).
Acceptance: `ScriptingDefinesTests.StandaloneDefines_AllHaveAConsumer` (EditMode): every Standalone define is named by a source file under Assets/, Packages/ or Library/PackageCache. Written first; fails on main today (two dead symbols).
Must not break: the suite compiles and passes with the symbols gone (nothing under Assets/ or Packages/ references them, grep 2026-09-17).
Revert: `git revert`.
Feel change? no

### CHG-013 HoverTank preset: host the corner cubes on cubes, not on hoverblades — status: LANDED 2026-09-17 (docs/changes/183; approved by Grey, merged with main @ 64c715ef and re-run green before the merge; the class rule that held it changed with the approval, see CHARTER I1) (built on chg/013-hovertank-preset @ 259f0b03, suite green; red team KILL on class 2026-09-17: a structure cube's Up rotates its transform (BlockGrid.cs:181) and the cube's plank albedo is UV-mapped (BlockMat_Structure → PlankOak_Inv.png, `_ArtisticProjection: 0`), so four deck tiles of the friendly tank change grain direction; foreground arbitration: the kill stands, the diff is correct and complete and lands byte-for-byte on Grey's check-off; NEEDS-GREY APPROVE CHG-013)
Class: ASK (I1: anything visible). Was AUTO on the premise "a cube's Up is invisible"; the red team showed the premise false (.utmp/factory/gate/CHG-013-redteam.md Q4). Feel change: visible only (four inset deck tiles cross-grain); a PLAY entry after landing asks Grey to look at the deck once.
Pillar or readiness item: LAUNCH-READINESS T1 (the quarantine in PresetBlueprintTests.KnownInvalid must be emptied) and the presets' "equivalent to player saves" property (docs/changes/README.md § Known unknowns; pillars: pickup-and-play presets a pilot can trust).
Source: F-026 (found by CHG-008's coverage).
Change: in Blueprint_DefaultHoverTank.asset set the Up of the four corner cubes at y=0 so each is hosted by an adjacent cube instead of the hoverblade beneath it (Up=-X for the x=-2 corners hosts them on x=-1; Up=+X for the x=1 corners hosts them on x=0; verify those cells hold cubes); %s; remove the KnownInvalid entry and fix its hint text (it wrongly names scaffolder authoring); pin the quarantine pattern for any future entry by asserting the known error text, not just IsValid == false (red team note 1); pass the library into `DumpAllPresets_WritesAsciiSnapshot` so the snapshot cannot print "Validation: OK" for a preset the suite quarantines, and land the regenerated docs/blueprint-snapshots/presets.md (88 lines behind: no Grappler or Hover Tank section); fix the two stale strings (the comment "every preset asset path the scaffolder produces" and the guard's "scaffold via Build Everything" message), since HoverTank is not scaffolded (red team notes 3-4).
Acceptance: `Preset_PassesValidation(Blueprint_DefaultHoverTank)` Passed and `ScriptedChassisBuilderTests.EveryShippedPreset_PassesLibraryAwareValidation` Passed with KnownInvalid empty (the quarantine's own assertion flips first: written before the fix, it fails once the preset passes). Suite green.
Must not break: INV-2 (the blueprint's entries stay the same cells and block ids; only orientation metadata changes); the hoverblades' own Up and the connectivity result (rule 3) unchanged; the ASCII snapshot (docs/blueprint-snapshots/presets.md) identical for HoverTank apart from nothing (cubes have no orientation glyph) — confirm by diff.
Revert: `git revert`; the quarantine comes back with the failing preset.
Feel change? no (orientation of symmetric cubes)

### CHG-012 SurfaceNets benchmark: warm the Burst job, gate on the best of three medians, log a harness row — status: SPEC
Class: AUTO (I1: a flaky test fixed; an instrument). The 1 ms number is Grey's (docs/subsystems/terraforming.md § 12) and does not move.
Pillar or readiness item: LAUNCH-READINESS T1 (F-021: the suite is green with a coin in it); D2 (a flaky test is a bug); D6 (a proxy carries its band, and the band becomes a row in the log instead of a memory).
Source: F-021; SPIKES L3 (isolated, n=6, medians 0.487–0.609 ms) and L3b (under full-suite load, n=5, 0.360–0.376 ms, lower than isolated; the single 1.005 ms followed a full recompile, so a cold Burst job or a cold CPU is the leading cause and load is ruled out).
Change: Assets/_Project/Tests/PlayMode/Voxel/SurfaceNetsBenchmarkTests.cs, plus `PerfBaselineHarness.AppendToLog` made `internal` (same asmdef, Robogame.Tests.PlayMode) so both rows go through the one writer, plus docs/subsystems/terraforming.md:611 (a tier-2 doc that says `dim=33` and "over 50 iterations" where the code says 34 and now three windows). (a) The untimed warm-up in the median test rises from 3 to 10 iterations AND runs with `BurstCompiler.Options.EnableBurstCompileSynchronously = true` (saved and restored), because the reproduction showed the real mechanism: Burst compiles the job asynchronously, and with a cold Library/BurstCache the whole 50-iteration window runs on the managed fallback (rig @ 6a5456a3, fixture alone, cache deleted before each run: medians 1.973 / 1.971 / 1.810 ms, 3 of 3 failing; warm: 0.36–0.61 ms); ten warm-up calls alone cannot cover a compile that takes seconds, a synchronous compile in the warm-up can. The PlayMode test asmdef gains a `Unity.Burst` reference. (b) The timed measurement becomes three consecutive windows of 50 iterations each; the gate asserts `min(median_1, median_2, median_3) < 1.0 ms`: a real regression is slower in all three windows, a transient dip (a compile, a P-state, a neighbour) is not; the assertion message prints all three. (c) Every run appends one row to docs/perf-captures/harness-log.txt through the same `Application.dataPath/../docs/perf-captures` path `PerfBaselineHarness.AppendToLog` uses (:210-218), formatted `[SURFACENETS-BENCH] dim=34 warmup=10 windows=3x50 medians=a/b/c ms best=x ms min=y ms max=z ms`, so drift is a number in the log. (d) The test is renamed `Mesh_Dim34Sphere_BurstMedianUnderOneMillisecond` and its docstring says dim=34 (32 cells + 2 apron; the constant has been 34 since Phase 2b; docs/changes/66:105 names the old name and is history, left alone). The GC-zero test is untouched.
Acceptance (tests-first, D4 (2); every run recorded in .utmp/factory/gate/CHG-012-suite.txt with its medians): (1) REPRODUCE on main's test before any edit: with the rig reset to the branch's base sha (LESSONS § METHOD 8), delete `.claude/worktrees/test-rig/Library/BurstCache` (a rig cache, not an edit of the tree) and run the fixture alone (`-testFilter SurfaceNetsBenchmarkTests`, the SPIKES L3 procedure) 3× cold; record the medians. If no cold run reaches 1 ms, the cold-Burst hypothesis is falsified: write SPIKES L6 with the numbers and re-spec against what the cold logs show; do not land a fix for a cause that did not reproduce. (2) With the change: the same 3 cold-cache runs PASS and log three rows; 2 warm runs log rows whose best median lies inside the L3/L3b envelope (0.36–0.61 ms); the five rows are copied from the rig's harness-log.txt into the clone's and land with the change (batch-rig rows, labelled as such by the row's context, ENGINEERING RULE 7). (3) `run-tests.sh All` green.
Must not break: the GC-zero test; the 1 ms target (Grey's, terraforming.md § 12); INV-7 (no perf claim beyond the rows); nothing at runtime (test assembly only).
Revert: `git revert`; the coin comes back.
Feel change? no

### CHG-014 docs/TRACES.md rebuilt from the Editor; the doc-drift sweep checks it — status: SPEC
Class: AUTO (I1: doc drift against code, dangling TRACEs; the file is generated).
Pillar or readiness item: docs current (D13: "the TRACE index"); F-023.
Source: F-023 (red team on CHG-002).
Change: replace docs/TRACES.md with the output of Robogame → Traces → Rebuild Index, run over the live bridge on the factory Editor at main @ 8751b11e on 2026-09-17 (console: "[Traces] Wrote docs/TRACES.md — 111 traces, 0 dangling."; the output is parked at .utmp/factory/sweeps/TRACES-rebuilt-2026-09-17.md): 79 insertions, 20 deletions, i.e. the ADR-0008 and ADR-0009 sections that were never indexed, line numbers moved by later edits, and HudPointerGuard's retargeted trace (CHG-002). Then add docs/TRACES.md to the sweeper's doc-drift sweep (.claude/agents/sweeper.md: after Traces Validate, run Rebuild Index and report a non-empty `git diff --stat docs/TRACES.md` as one inconsistency finding, then `git checkout -- docs/TRACES.md`). Two files, one intent: the index says what the code says and stays that way.
Acceptance: on the branch, a second Rebuild Index over the bridge leaves `git status` clean (the index is deterministic and current) and Validate logs 0 dangling; the suite is untouched (no C# change) but runs as the chain requires.
Must not break: nothing at runtime; the file is generated and read by humans only.
Revert: `git revert`; the 17 stale sites come back.
Feel change? no

### CHG-015 the test rig must not kill the factory's MCP server — status: SPEC
Class: AUTO (I1: tooling, harnesses and instruments; a console-facing fault fixed on the rig; nothing a player sees).
Pillar or readiness item: the rig (D7): the bridge is the loop's console, screenshot and profiler instrument, and F-031 shows every batch test run takes it down for the rest of the Desktop session (ASSUMPTIONS #8: a Desktop session cannot re-dial).
Source: F-031 (shift 5; the server log, the emptied pidfile dir, the Editor log).
Change: `.claude/scripts/run-tests.sh` step 1, after the sync and before the batch run: remove the `com.coplaydev.unity-mcp` entry from the RIG worktree's `Packages/manifest.json` (and its block in `Packages/packages-lock.json`, so UPM does not re-resolve it) with a small in-script edit (`python` is on the rig per LOOP-STATE § RIG; fall back to `sed` if not). The rig is clobbered by the sync every run (ENGINEERING RULE 6), so the edit never reaches the clone or main. The package is editor-only and no test assembly references it (grep of Assets/_Project/Tests for `MCPForUnity`: 0 hits, 2026-09-17), so the suite's content is unchanged; the rig also loses the package's `[TestRunnerNoThrottle]` hook, which only toggled the Editor's interaction mode. Record the rule in `docs/loop/LESSONS.md` § METHOD and in LOOP-STATE § RIG. Upstream: file the defect with CoplayDev (the handshake belongs in SessionState or a project-scoped store); until then the loop restarts the server by hand when 8080 is silent (command: `uvx --from <package's Server url> mcp-for-unity --transport http --http-url http://127.0.0.1:8080 --project-scoped-tools`, as ServerCommandBuilder.TryBuildCommand builds it).
Acceptance (tests-first, D4 (2); recorded in .utmp/factory/gate/CHG-015-rig.txt): (1) REPRODUCE: the natural reproduction is F-031's timeline (an Editor-launched, handshake-tracked server on 8080 died at 12:54:26Z while the builder's batch run quit, with no other actor); the isolating control is a server started BY HAND (no handshake) surviving the red team's two batch quits on the same rig (18:04Z onward; `netstat -ano | findstr :8080` before and after, with times). If the hand-started server also dies at a batch quit, the handshake theory is wrong and the spec is withdrawn for a re-read of ProcessDetector/ProcessTerminator. (2) With the change: restart the server, run `run-tests.sh EditMode` three times; 8080 listens after each; the rig's manifest shows no MCP entry during the run and the rig log shows no `MCP-FOR-UNITY` line; suite counts identical to the previous green (EditMode 537/537 on the CHG-013 base, PlayMode 152/153). (3) The wall time of `run-tests.sh All` recorded beside the 1m28s baseline (the package's absence may change it; a number, not a claim).
Must not break: the rig's sync semantics (tracked diffs + untracked files still copied); `run-tests.sh` exit codes; nothing in the clone or on main is edited by the script.
Revert: `git revert`; the rig kills the bridge again.
Feel change? no

### CHG-017 the changes index keeps up with the ledger — status: SPEC
Class: AUTO (I1: doc drift against code, "the changes index" by name; plus an instrument, the landing chain).
Pillar or readiness item: docs current (D13); CLAUDE.md § Active work sends every session to docs/changes/README.md for "the current session's intent", and its table stops at 131 while the ledger is at 179 (F-036).
Source: F-036.
Change: (a) `land.py land` gains step 6b: insert `| NNN | [<title>](NNN-<slug>.md) |` directly under the header separator of the "## Sessions (newest first)" table in docs/changes/README.md, where `<title>` is the `--title` argument; `--dry-run` prints it; a new unit test in .claude/scripts/factory/tests/test_land.py asserts the row lands at the top of the table and that a README without the table makes the chain refuse (fail loud, never silently skip). (b) A one-off backfill script (kept under .claude/scripts/factory/ as `backfill_changes_index.py`, idempotent: it adds only rows whose number is absent) generates rows 132-179 from each entry's first heading with the `# NNN — ` prefix and any ` (LOG-NNN)` suffix stripped, newest first; its output is reviewed by eye once and committed. docs/changes/README.md's own "Recent batch" prose is not touched (it is Grey's narrative, not the index).
Acceptance: `python -m unittest discover -s .claude/scripts/factory/tests -t .claude/scripts/factory/tests` green with the new test (written first, red on main); after the backfill, `grep -c '^| 1[3-7][0-9] |' docs/changes/README.md` = 48 and the top row is the highest-numbered file; the next real landing's row appears without a hand edit (verified on the first landing after this one).
Must not break: land.py's existing refusals and dry-run (its tests); the README's other sections byte-identical (diff limited to the table).
Revert: `git revert`; the table stops at 131 again.
Feel change? no

### CHG-019 ContinualTraces scans traces anywhere in a comment; the doc-drift sweep's index step has rules — status: SPEC
Class: AUTO (I1: tooling and instruments; doc drift). Red team: N/A under D16b (an Editor-only tool under Assets/_Project/Scripts/Tools/Editor and a sweep brief; no shipped runtime code, no NOD action, no invariant touched); the green suite is the gate.
Pillar or readiness item: docs current (D13: the TRACE index); INV-2 traceability (its only anchor is invisible to the index today).
Source: F-041, F-042 (red team on CHG-014).
Change: (a) `Assets/_Project/Scripts/Tools/Editor/ContinualTraces.cs`: the marker regex (line 40-41, `//\s*TRACE\[…`) only matches `TRACE[` immediately after `//`; widen it to match a marker anywhere after a comment start, on `//` and `///` lines alike (`//.*?TRACE\[\s*(?<id>[^\]]+?)\s*\]\s*:?\s*(?<note>.*)$` or equivalent), so `// keeps INV-2 honest: TRACE[INV-2]: …`, `/// TRACE[ADR-0003]: …` and `int x = 1; // TRACE[LOG-5]: …` all scan; keep the ContinualTraces.cs self-exclusion (:140) and the id resolution unchanged; factor the match into `public static bool TryParseMarker(string line, out string id, out string note)` used by the scan loop, so it is testable without the file system. (b) `Assets/_Project/Tests/EditMode/Tools/ContinualTracesTests.cs` (new; the EditMode asmdef already references Robogame.Tools.Editor): cases for the four line shapes above plus a negative (`// no marker here` → false) and the self-exclusion is not the test's concern. Written FIRST: the mid-comment and `///` cases fail on main's regex. (c) docs/TRACES.md regenerated with the widened scanner: from the batch rig, `Unity.exe -batchmode -nographics -projectPath <test-rig> -executeMethod Robogame.Tools.Editor.ContinualTraces.RebuildIndex -quit -logFile <log>` after a `run-tests.sh` sync (if the menu method is not directly executable, add a static `RebuildIndexBatch()` wrapper), then copy the rig's docs/TRACES.md into the clone; expected: the 11 mid-comment sites and the 3 `///` sites join the index (111 → ~125 traces; the exact count is recorded), `## INV-2` appears with BuildModeController.cs:92, and Validate reports 0 dangling (any newly indexed id that does not resolve is fixed in the same change or reported as a finding, not hidden). (d) `.claude/agents/sweeper.md`: the doc-drift step says `BRIDGE_DOWN` and stops when `execute_menu_item` fails; the closing "What you DON'T do" section names the one write-and-restore it performs (docs/TRACES.md, restored with `git checkout --`); the step checks the live Editor's project is this clone (the `mcpforunity://instances` resource or `manage_scene get_active`'s path) before trusting the diff.
Acceptance: `ContinualTracesTests` green (5 cases) with the two written-first cases red on main; `run-tests.sh All` green; docs/TRACES.md carries `## INV-2` and the count line names ≥ 122 traces, 0 dangling; a second batch rebuild is a no-op diff.
Must not break: the existing 111 sites stay indexed with the same lines and notes (diff of the old rows against the new file: only additions); Validate's id kinds unchanged; nothing at runtime (Editor assembly only).
Revert: `git revert`; the 14 sites drop out of the index again.
Feel change? no

### CHG-020 Server-first bridge: the launcher serves 8080 before any session dials it; mcp_http.py uses the bridge with a dead connector — status: BUILT (branch `chg/020-server-first-bridge`, plumbing-built from main, no checkout; 32/32 factory unit tests in a scratch copy; suite N/A until the rig is free)
Class: AUTO (I1: tooling, harnesses and instruments; nothing under Assets/). Red team N/A under D16b (no shipped runtime code, no NOD-list action, no invariant); the green suite plus the factory unit tests are the gate.
Pillar or readiness item: LOOP-STATE § RIG and BACKLOG 12 (the bridge must be up before the loop's session is opened); D7 (one truth test, the live Editor, reachable every shift); Grey's fire-and-polish steer 2026-09-17 (no terminal steps, no Unity juggling).
Source: F-017 and LESSONS 2 (paid for on shifts 3, 4 and 6: the session dialled before 8080 listened and stayed bridge-down all shift); ASSUMPTIONS #12 (auto-start fires only once the Editor is focused; three observations); F-031/LESSONS 9 (a hand-started server carries no handshake and no Editor's quit cleanup can kill it); HttpAutoStartHandler reuses a reachable server instead of launching its own (the load-bearing fact for server-first); the runner session's HTTP proof 2026-09-17 (initialize → execute_code → mute=True on the clone, with the session's connector dead).
Change (7 files, all under .claude/):
- `.claude/scripts/factory/Start-Factory.ps1` step 6 rewritten server-first: (a) when 8080 is silent, start the package's own server command by hand — `uvx --offline --from mcpforunityserver==<Library/PackageCache package.json version> mcp-for-unity --transport http --http-url http://127.0.0.1:8080 --project-scoped-tools`, no `--pidfile`, no instance token, output to `.utmp/factory/mcp-server-manual.log` / `.err.log` — and wait up to 60 s for LISTEN; (b) start this clone's Editor only when no Unity.exe has `-projectPath <this clone>` (Grey's checkout never matches; batch Editors excluded); (c) foreground the Editor once (ShowWindow + SetForegroundWindow + AppActivate) as soon as its window exists, up to 10 min; (d) wait up to 5 min for the Editor to register on the server (`mcp_http.py wait-instance <clone folder name>`), which sets mode `editor`; otherwise `editor-starting` with a warning; (e) only then the Desktop prompt, green when the bridge is confirmed, yellow when it is not. `-NoEditor` skips (a)–(d). `-DryRun` prints the rig steps in order and starts nothing. Every existing preflight check is kept. One incidental hardening: `git rev-parse @{u}` on a branch with no upstream no longer aborts the preflight (it threw under `$ErrorActionPreference = 'Stop'`; found because the clone sat on a builder's branch during the dry run).
- `.claude/scripts/factory/mcp_http.py` (new, stdlib only, UTF-8 output): Streamable-HTTP MCP client — `ping`, `instances`, `tools`, `resource <uri>`, `call <tool> '<json>'`, `code '<C#>'`, `wait-instance <name> [seconds]`; exit 0 / 1 (tool error or isError) / 2 (no server). `MCP_HTTP_URL` overrides the endpoint.
- `.claude/scripts/factory/tests/test_mcp_http.py` (new, 8 tests, mocked urlopen): initialize → initialized → session header on every later message; SSE and bare-JSON framings; the `code` shorthand; exit codes 0/1/2; `wait-instance` returns on the factory's name and does NOT accept Grey's `robogame` for `robogame-factory` (a prefix must not match the other clone).
- `.claude/scripts/factory/README.md`: the launcher row and a new `mcp_http.py` row.
- `.claude/commands/robogame-factory.md` step 1c: a failed connector is not a dead bridge; probe with `mcp_http.py instances`, run the full ladder over HTTP when it answers, batch-only when it exits 2; never restart the Editor or the server from inside a shift.
- `.claude/agents/sweeper.md` (console sweep) and `.claude/agents/red-team.md` (console check): the same HTTP fallback before `BRIDGE_DOWN` may be returned.
Acceptance (tests-first, D4 (2); evidence in `.utmp/factory/gate/CHG-020-*.txt`):
(1) Unit: `python -m unittest discover -s tests -t tests` in `.claude/scripts/factory/` → 32/32 (24 existing + 8 new); the negative proof is the last test: an instance named `robogame` must not satisfy `wait-instance robogame-factory`.
(2) Launcher, dry: `Start-Factory.ps1 -DryRun -Desktop` from the clone prints the rig steps in order (server → Editor → foreground → wait-instance) and starts nothing; run against this clone 2026-09-17 with the server and Editor already up, it reported both as present and listed only the foreground and wait steps.
(3) Server command, live: the pinned offline command started a second server on port 8081 in under 25 s, answered `initialize` and the `mcpforunity://instances` resource (0 instances, as expected on a spare port), and was stopped. It never touched 8080.
(4) Client, live: against the shift-6 server on 8080, `ping`, `instances` (the factory's Editor listed), `wait-instance robogame-factory` (0.0 s), `wait-instance nosuch 3` (exit 1), `read_console`, `execute_code` all behaved as documented.
(5) The real test is the next shift start: `Start-Factory.ps1 -Desktop` with nothing up must end with `bridge live: {"ok": true, "instance": "robogame-factory@..."}` and the session opened afterwards must show `UnityMCP: connected`. That is the first wake's job to record (BACKLOG 12 closes on it).
(6) `run-tests.sh All` green (nothing under Assets/ changes; the run is the D2 formality).
Must not break: the existing preflight order and refusals (Grey's checkout, live lock, merge in progress, missing tools); the tab launch path (unchanged after the rig block); CHG-015 (the rig still strips the package; a hand-started server has no handshake to be killed through); Grey's own Editor (never started, never foregrounded, never matched by name).
Revert: `git revert`; the launcher goes back to Editor-first and the sessions go back to dialling by luck.
Feel change? no (tooling on the factory machine)
Owed / follow-ups (not in this change): D-007 on the board (a login-time task that runs the same server start so 8080 is up before Grey opens anything); the Desktop-shift lock never gets a pid, so every later preflight calls it "stale" after 3 minutes — a one-line finding for the sweep, not fixed here.

## BUILT THIS SHIFT (moved to docs/changes on landing; tally for HEALTH)

- shift 2, 2026-09-16: nothing built; the shift ended on the plan cap before rung 1.
- shift 3, 2026-09-17: CHG-003 landed (docs/changes/174); CHG-002 landed (docs/changes/175-doc-drift-sweep.md); CHG-005 landed (docs/changes/176-delete-unused-packs.md); CHG-001 landed (docs/changes/177-provenance-records.md).
- shift 4, 2026-09-17: CHG-008 landed (docs/changes/178-preset-coverage.md); CHG-010 landed (docs/changes/179-dead-defines.md).
- shift 5, 2026-09-17: CHG-015 landed (docs/changes/180-rig-strips-mcp.md); CHG-012 landed (docs/changes/181-surfacenets-gate.md); CHG-014 landed (docs/changes/182-traces-index.md); CHG-013 built, red team KILL on class → APPROVE-WAIT.
- between shifts, 2026-09-17: CHG-013 approved and landed by Grey's runner session (docs/changes/183-hovertank-preset.md).
- shift 6, 2026-09-17: CHG-019 landed (docs/changes/184-traces-scan.md); CHG-017 landed (docs/changes/185-changes-index.md); CHG-020 landed (docs/changes/186-server-first-bridge.md, built by the peer session); land.py learned the red-team N/A record (f03913c9).
